// B2B customer portal theme — a clean, LIGHT palette. Deliberately distinct from
// the dark slate/navy internal `C` palette: this surface is customer-facing, so
// it should feel like a modern, trustworthy storefront rather than an internal
// ops console. Keep it cohesive — one accent, neutral greys, generous whitespace.
export const B = {
  bg:        "#F7F8FA",   // app background — near-white grey
  surface:   "#FFFFFF",   // cards, panels
  surfaceAlt:"#F1F3F7",   // subtle fills, hover
  border:    "#E2E6EC",   // hairline borders
  header:    "#FFFFFF",   // top bar
  primary:   "#CC2200",   // ROF red — primary actions + links (matches launcher)
  primaryHi: "#A81B00",   // hover
  text:      "#111827",   // gray-900 — body text
  textSub:   "#374151",   // gray-700 — labels
  textMuted: "#6B7280",   // gray-500 — secondary / placeholder
  danger:    "#B91C1C",
  dangerBg:  "#FEF2F2",
  dangerBdr: "#FECACA",
  shadow:    "rgba(17,24,39,0.08)",
  font:      "system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif",
};
