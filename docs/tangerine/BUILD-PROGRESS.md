# Tangerine ERP — Live Build Progress

> **Single source of truth for "% complete."** Update this doc whenever a phase or module lands (it's part of the PR, like the user-guide chapters). Roadmap: `project-erp-build-roadmap` memory + `docs/tangerine/` arch docs. 25 phases (P1–P25), 49 modules (M1–M49), 7 pre-existing apps (E1–E7). **The 3 pre-existing operational apps that feed Tangerine — ATS, PO WIP (Tanda), Inventory Planning — are documented in [`docs/apps/`](../apps/README.md).**

**Last updated:** 2026-07-10 (Month-End Close module — per-period close checklist: 8 automated tie-out checks (`close_run_auto_checks` SQL battery) + 6 manual sign-offs + audited period locking; **latent `gl_post_year_end_close` 100× dollars-as-cents bug fixed before first use**)
**Last updated:** 2026-07-11 (#xoro-account-truth RUN — full-history walk enriched **3,661/3,680 register bills** with Xoro's own GL accounts (only unresolved name = Xoro's catch-all 'Other Miscellaneous Expense' $3.16M → CEO mapping table); xoro-verify recon of the $8.88M 8007-origin book: **MATCH $883,412.66 / DIFF $949,801.59 → 47 correction JEs posted / UNMAPPED $234,557.56 / NO-SIGNAL $6,807,647.01** (header-only expense bills, REST exposes no truth); biggest fix: $813.6k mis-defaulted to 6343 → **1201 Inventory** (Xoro types the lines Inventory) + 2253 Apparel/Interland $41.9k out of 8007 → 1201; FedEx/UPS 5405→5403/5402 splits, Shopify→6705, Freelancer→6723, Fineline→5023; re-run converges DIFF $0.00; **8007 $1,285,433.56 → $1,243,518.81**, 2000 unchanged, imbalance $0.00, 1,170/1,170 JEs balanced; United Aryan ROF-B005300 = header-only via REST → stays PENDING in 1308)
**Last updated:** 2026-07-12 (#1699 Planning — **Edit run** (name + horizon/snapshot dates, with **Save & rebuild** routing into the preserve-vs-wipe dialog when already built) + **0-row build guard**: a build that writes 0 forecast rows now shows a clear error explaining the likely cause (leftover grid filter / period filter outside the horizon) instead of a silent "success", and a period filter whose months don't overlap the run's horizon is ignored-with-warning rather than silently nuking the whole build. Root-caused a real 0-row build: "Ross Q1-Q2" (Dec2026–Aug2027 horizon) had 1,450 eligible pairs but wrote 0 — scope matched nothing)
**Last updated:** 2026-07-12 (#1696 private-label parts tie to Xoro — surf-shop private-label labels/patches (BP/FL/JK/ML/HS/FL00002) carry blank or shop-shared UPCs in Xoro so they could never tie via the UPC spine (the residual on-hand "drift"). Now resolved by their unique **ItemNumber** (= sku_code) in both `sync-onhand-spine.mjs` and the nightly `verify_tangerine_onhand.py` tie-out; every label style now reconciles to Xoro to the unit and self-maintains nightly. Plus: 6 case-duplicate surf-shop customers merged, 26 `style_customer_numbers` links completed, 55 empty dashless/abbrev twin SKUs dedup'd (all reversible). Also fixed the tie-out silently no-op'ing in the nightly — it had no anon-key source, so the [TANGERINE DRIFT] alert could never fire; embedded the read-only anon fallback)
**Last updated:** 2026-07-11 (#xoro-account-truth — CEO NON-NEG: Xoro's GL is the 100% classification source of truth. Bill feed now carries the GL account Xoro posted each line to (`Expense Account` + `Item Type`; `--full-history` walks Open+Paid+Partially-Paid — `bill/getbill` hides paid bills by default); `/api/ap/sync-bills` stores + exact-match resolves it (`xoro_expense_account_name`/`expense_account_id`, `xoroAccountMap.js`; #1668 register bills enriched lines-only); AP sweep precedence now **bill Xoro account > vendor default > 8007**; `reclass-8007.mjs xoro-verify` recons every (vendor, month) 8007 bucket vs Xoro truth → MATCH / DIFF (correction JEs `:xoro-correction`) / UNMAPPED (`xoro-account-name-map.csv`) / NO-SIGNAL)
**Last updated:** 2026-07-10 (8007 round 3 — vendor DEPOSITS: **United Aryan $80k + Luxury Collection $25k → DR 1308** (`vendor_prepayment_reclass`, open-deposits schedule `ap-vendor-deposits.csv`); CNX deposits verified ALREADY-APPLIED via #1668 relief CR 1308 (no double-count); $54k credit-settled bills flagged NET-ZERO vs 5005; **8007 now $1,285,433.56**)
**Last updated:** 2026-07-10 (8007 cleanup round 2 — **Factory 1 confirmed inventory: $623,414.04 → DR 1201** (17 monthly `vendor_inventory_reclass` JEs) + CEO-authorized auto-set of **145 more vendor defaults** → 576 new JEs $1,401,163.16; **8007 now $1,390,433.56** = INVENTORY? $890.7k awaiting CEO confirm list + FLAG $496.1k related-party/financing + $3.7k zero-signal)
**Last updated:** 2026-07-10 (8007 Uncategorized Expense cleanup — vendor default expense accounts set for 62 HIGH-confidence vendors + **523 monthly reclass JEs posted, $5,224,663.22 moved into 39 real expense categories; 8007 $8.88M → $3.65M**; Rosenthal $859k flagged, $2.79M in CEO review CSV)
**Last updated:** 2026-07-10 (Sep–Dec 2024 AR historical backfill — **5,350 invoices / $8,302,559.57 posted, every month ties the Xoro registry to the cent; AR history now starts 2024-09-01**; receipts pipeline unjammed — first-ever `ar_receipt_xoro` JEs, 5,331 posted)
**Last updated:** 2026-07-10 (#1676 3-Way Match module — bill-grain PO↔receipt↔bill match engine over the full AP book (3,719 bills), tolerance config, nightly 06:45 UTC cron, exception queue with Accept-variance/Dispute (T11), backfill run on prod)
**Last updated:** 2026-07-10 (#1677 Factor customer links — 12 of 13 Rosenthal numbers linked to customers (Macys Corporate Services flagged for CEO) + **AP AmountPaid delta watcher** — nightly 06:30 UTC cron guards GL 2000 against post-export Xoro payments; register-comparison mode, idempotent, Sync Health row; first prod run: 3,680 bills checked, 0 deltas, 0 anomalies)
**Last updated:** 2026-07-09 (Factor Module Phase 2 — chargeback detail + dispute tracking (11 months, 5,928 items, every month ties), 12 AR snapshots through 2026-06, **12 factoring-cost JEs posted $515,690.72**)
**Last updated:** 2026-07-08 (Factor Module Phase 1 — Rosenthal statements + open-AR detail imported and tied to the cent, Factor (Rosenthal) panel with GL 1107 tie-out)
**Last updated:** 2026-07-09 (#1668 AP bill-history backfill — full Xoro Bills register (3,680 bills $51.08M) + 2,685 payments ($41.13M) posted; **AP 2000 ties to the register to the cent: $9,947,831.51 CR**; #1665 AP waiver lifted → ok)
**Last updated:** 2026-07-09 (Drill-through Phase 2 — AR/AP aging bucket drills, Segment P&L → GL accounts, bank-recon JE/GL links)
**Last updated:** 2026-07-09 (#1671 Bank reconciliation mirror — 13 accounts, 2,551 Xoro register txns mirrored, **100% matched; 285/286 closed account-months reconciled to the cent** through 2026-05-31; nightly sync + CASH tie-out in the 06:00 monitor; Plaid seam documented)

## Summary

| Metric | Done | Total | % |
|---|---|---|---|
| **Phases / slots (P1–P25)** | **14 code-complete** (P1–P10, P12, **P13** build+GL, P15, P16) + **9 MVP/🟡** (P11, P17–P22, P24, P25); only **P23** (Xoro cutover, operator-gated) + the P14 PLM-ext slot remain ⬜ (RBAC filled P14). Weighted by scope **≈78%**; 14/25 fully shipped = 56% | 25 | **~78%** |
| **Modules (M1–M49 + ➕M50)** | ~43 + ➕M50; prior set + **M23 Returns/RMA · M49 Drop-ship · M13 3PL · M14 EDI (surfaced) · M9 reporting hub · M20 1099 · M21 Fixed Assets · M22 Budgets**; remaining: M15 Public API, M19 Sales Tax, M46 BI charts, M32/M33 PLM-ext | 49 (+ins) | **~78%** |
| **Path to Xoro retirement (P1–P23)** | through P12 + P14/P15 + P16 core + P18 portal + **P13 build+GL complete** + P17 planning surfaced + P19–P22 MVPs; **P23 cutover itself = 0%** (operator parallel-run, first gate Cash ~2026-07-28) | 23 | **~78%** |

> Note: the **P14/P15 slots** hold operator insertions (RBAC, Brand Master), not the original roadmap scope (PLM-ext, Pricing — deferred). The brand / allocation / partition work is **built but GATED** (`BRAND_SCOPE_MODE` off) — shipped code, not yet *enforced* in prod.

> **Two important caveats when reading the %:**
> 1. **The done half is the hard half** — full dual-basis (accrual+cash) accounting, FIFO inventory, 5-yr AR backfill, close/financials, bank recon, and all three revenue integrations. Remaining phases are more numerous but individually lighter.
> 2. **Code ≠ Xoro off.** Retirement (P23) is gated by **calendar-floor parallel runs** — Tangerine must reconcile against Xoro within tolerance for ~2 consecutive months *per area* before each cutover. Actual retirement is paced by parallel-run verification, not just shipping code.

Legend: ✅ done · 🟡 in progress / partial · ⬜ not started · ➕ operator insertion (off original numbering)

> **Recent cross-cutting landings (2026-07-10)** — not tied to a single phase row:
> - **Month-End Close module** — best-in-class close discipline: a per-period close checklist with automated tie-outs, manual sign-offs, and audited period locking. New tables `close_periods` (one per (entity, gl_period); status open → in_close → closed; entity default `coalesce(current_entity_id(), rof_entity_id())`; T10 `source` tagging; T11 `audit_row_changes` triggers; auth-only RLS) + `close_checklist_items` (unique `(close_period_id, item_key)`; kind auto|manual; status pending|pass|fail|signed_off; the numbers behind every verdict in `detail` jsonb; `signed_off_by/at` + required note). **8 automated checks** computed by the read-only SQL RPC `close_run_auto_checks(entity, period)` (all aggregation in SQL — no PostgREST caps; #1665 tie-out semantics): GL balanced per basis to the cent · AR subledger tie 1105/1107/1108 · AP 2000 tie (with the `pending_payments` waiver) · bank/CC recon runs all `reconciled` · no draft/unposted JEs · 8007 Uncategorized activity = $0 · factor statement Net OAR = GL 1107 as of period end (when a statement covers the month) · revenue posted > $0. **6 manual sign-offs** (bank statements / factor statement / chargebacks / payroll / depreciation / controller) — note required, who/when recorded, revertible with reason while open. **Close period** requires ALL checks pass + ALL sign-offs, then locks `gl_periods` → `closed` through the audited `gl_period_transition_status` RPC (reason mandatory; `je_period_lock` triggers enforce); **Reopen** = admin-only + mandatory reason, checklist survives. Panel **Tangerine → Accounting → Month-End Close** (`month_end_close`): 12-month close-calendar strip, per-item drill (detail numbers), sign-off buttons, xlsx export. Handlers `/api/internal/month-end-close/{periods,checklist,run-checks,sign-off,close,reopen}` (authenticateInternalCaller; identity via resolveUserId JWT→X-Auth-User-Id; RBAC segment → `gl_periods`, close/reopen = `post` action). **Also fixes the latent `gl_post_year_end_close` 100× bug** (P5-6, never run): the closing-JE aggregation labeled raw `journal_entry_lines` DOLLAR sums `amount_cents` then divided by 100 — every closing JE would have posted at 1/100 of the true amount; now `ROUND(SUM(amount_dollars) * 100)` per the 20260970000000 rule, plus a T11 `audit_reason` on the closing-JE payload (the D3 reason-required trigger would have rejected the post). Migration `20260972000000` + 8 vitest shape tests. Prod dry-run of the checks for 2026-05/2026-06 (read-only — no periods closed). User-guide ch03.
> - **8007 round 3 — vendor deposits (prepayments before the invoice).** CEO: "some of these look like prepayments prior to receiving the invoice." Every confirm-list vendor's register rows probed (round amounts, cash-paid, vendor-bill ref = payment date, absorbing-invoice search, and the register's prepayments/credits relief columns). (1) **OPEN deposits → 1308**: United Aryan (EPZ) $80,000.00 (paid 2025-12-05, ref '12052025') and The Luxury Collection $25,000.00 (paid 2026-02-25, ref '022026') reclassed **DR 1308 Vendor Prepayments & Deposits / CR 8007** at source months, new `journal_type='vendor_prepayment_reclass'` + `PREPAYMENT_OPEN` script tier; no COA migration needed (1308 exists from #1668, postable/non-control). Open-deposits worklist committed: **`docs/tangerine/ap-vendor-deposits.csv`** (vendor, date paid, amount, age, evidence). (2) **APPLIED deposits — verified NO double-count**: CNX America's big bills ARE the goods invoices (B005513 $209,690.17 with $25,000 deposit applied; B005662 $136,913.72 fully deposit-settled); the #1668 relief JEs already CR'd 1308 for exactly those amounts (verified on prod), so the 8007 DR carries cost once — nothing eliminated. (3) **NET-ZERO** (never posted): Dynamic Full $40,000, Anhui $14,012.64, NEXT ELEVATION $79,083.30, Mass Apparel $0.70 — bills fully settled by vendor credits/discounts; their 8007 DR is exactly offset by the same bill's relief CR 5005; true-credit vs deposit-as-credit is a controller call, flagged in the review CSV. (4) Go-forward deposit routing stays MANUAL (no reliable deposit marker in the feed; never default a goods vendor to 1308). Verified: **8007 $1,390,433.56 → $1,285,433.56**, 1308 CR $5,062,725.55 → **$4,957,725.55**, imbalance $0.00, 1,122/1,122 reclass JEs balanced, 0 reclass lines on 2000. Residual: INVENTORY? $731,701.24 (8 vendors, per-bill evidence in CSV) + FLAG $496,050.43 + NET-ZERO $54,013.34 + LOW $3,668.55. User-guide ch13.
> - **8007 cleanup round 2 — Factory 1 inventory reclass + CEO-authorized default auto-set.** Follows #1675/#1679 (Rosenthal → 6802, $859,158.88 swept after the CEO deleted the 12 statement-derived factor_cost JEs). (1) **Factory 1 CONFIRMED inventory** (CEO 2026-07-10): its $623,414.04 of 8007 charges reclassed **DR 1201 Inventory / CR 8007** across 17 source months, new `journal_type='vendor_inventory_reclass'` (BS repair, not a P&L category move — those goods' sales already relieved 1201 via AR COGS legs at avg cost, so the missing purchase-side DR understated inventory). Script gains an `INVENTORY_CONFIRMED` tier (default also set to 1201 → go-forward sweep routes future non-item lines to inventory; 1201 is ROF-postable + non-control so it passes the #1666 sweep validation) and an `INVENTORY_SUSPECT` list (13 vendors, $890,714.58 — CNX America $355k, Interland $223k, NEXT ELEVATION $103k, United Aryan $80k, …) that is **never auto-posted** pending the CEO confirm list. (2) **Auto-set expansion** ("auto set the vendor expense accounts for the vendors you have bills for"): 145 more vendor defaults set by name classification (waste→6310, supplies→6354/6374/6381, comp-shopping retail→5206 Samples, individuals→6130 Contractors, restaurants→6349, travel→6370/6351/6332, marketplace fees→6384 (6510/6520/6524 are entity SAG), trade shows→6604 (5140 is SAG), customs duty→6386 (5110/5120/5130 are SAG), Spencer Lem→6134 + other tailor-mades) → **576 new (vendor, month) JEs, $1,401,163.16**. (3) `verify` hardened: structural 2000 guard (counts reclass lines on 2000 — 0) replaces the stale $9,947,831.51 constant; JE-balance check covers both reclass journal_types. Verified on prod: **8007 $2,791,596.72 → $1,390,433.56** (residual = INVENTORY? $890,714.58 + FLAG related-party/financing $496,050.43 (Venbrook, Bitton x2, SBA, FTB, life carriers) + zero-signal $3,668.55), trial-balance imbalance $0.00, **1,120/1,120 reclass JEs balanced**, 2000 unchanged by reclass ($10,061,433.54 before = after). Review CSV rewritten to the 33 remaining vendors. User-guide ch13.
> - **8007 Uncategorized Expense cleanup — vendor default expense accounts + monthly reclass JEs.** The per-bill AP engine (#1662/#1666/#1668) had parked ~**$8,875,418.82** of non-item bill charges in 8007, so the P&L showed one lump. New driver `scripts/reclass-8007.mjs` (`report` / `set-defaults` / `reclass` / `verify`, all `--dry-run`-able): probed 8007 by vendor×month (3,106 lines, 249 vendors, 2024-08→2026-07, all `ap_invoice_historical`), applied a conservative HIGH-confidence name→account mapping (62 vendors set on `vendors.default_gl_expense_account_id`, never overwriting operator-set defaults — go-forward sweep from #1666 now routes them automatically), and posted **523 (vendor, month) reclass JEs moving $5,224,663.22** into 39 real expense accounts (freight/3PL 6348/540x, medical/WC/auto insurance 633x, rent 6360, accounting/legal 6301/6344, software/EDI 6378/6718/6326, Google/Meta ads 6713/6614, commissions 6127/5105/6133, autos 6304, travel/meals, operator-set 6343 garment defaults). Hygiene: DR expense / CR 8007 **only** (zero lines touch 2000 — verified), source-month dating (month-end; current month uses latest source-line date), `journal_type='vendor_expense_reclass'`, source key `vendor_expense_reclass/<vendor_id>:<YYYY-MM>` (idempotent re-runs), audit reason on every post. Verified on prod: **8007 $8,875,418.82 → $3,650,755.60**, trial-balance imbalance $0.00, 523/523 JEs balanced, March-2025 P&L spot-check shows the new categories. Deliberately left: **Rosenthal & Rosenthal $859,158.88 EXCLUDED** (factor fees already booked by #1670 `factor_cost` JEs — double-count risk, CEO/controller to reconcile) + $2.79M MEDIUM/LOW vendors in `docs/tangerine/ap-8007-review.csv` (monthly totals + suggested accounts + reasons; garment mfrs likely inventory/COGS, related-party names, tax authorities, SBA principal-vs-interest, individuals). To finish: set defaults in Vendor Master per the CSV, re-run `reclass`. User-guide ch13.
> - **Sep–Dec 2024 AR historical backfill — the last 4-month sales gap is closed; AR history now starts 2024-09-01.** Loaded from the CEO's Xoro invoice-registry exports (12 files, 5,517 rows → 5,373 in-window invoices, $8,302,559.57) + verbatim item detail (`detail_full.csv`, 16,242 rows) via new `scripts/ar2024-backfill/` (stage → post → verify → fix-line-totals), reusing the EXISTING driver-v3 machinery end to end: `sales/sync-invoices` ingest (identical SKU/customer/channel/PPK/avg-cost resolution; 0 new items, 0 new customers — full resolution), `internal/ar-backfill/run` posting (routed 4005-4012 + COGS twins, AR class per customer, T11 reasons, `posting_date` = the invoice's real Xoro date), `ar_xoro_payment_state` + `ar-receipts-reconcile` receipts. **Posted: 5,350 invoices (23 registry $0 promo/internal invoices skipped by the runner's `total<=0` convention), 14,383 lines (10,406 SKU + 2,926 `AR2024-FREIGHT` top-ups $33,206.31 so every invoice's lines sum EXACTLY to its registry header + 1,051 `AR2024-NODETAIL` header-only summaries: Macys 970/$244,071.98 revenue-only consignment-style, wholesale 81/$1,168,374.25 with estimated COGS at the period blended cost ratio per channel — ROF 60.7% / PT 82.8%). All six verification gates PASS: every month ties the registry to the cent (Sep $1,820,754.10 / Oct $1,554,575.71 / Nov $2,696,785.79 / Dec $2,230,443.97), income_statement ACCRUAL margins 53.6/52.5/30.7/42.4%, GL imbalance 0.00, AP 2000 + 1105/1107/1108 tie-out diffs unchanged to the cent, 2025+ data untouched (23,404/48,759 baseline).** Two Xoro year-end re-issue pairs flagged for CEO: `ROF-I015528`↔`ROF-I015526` (Ross $262,028.85) and `ROF-I015522`↔`ROF-I015523` (Heritage $2,667.65) — identical customer/amount/FullPaymentDate, adjacent numbers, 12/31 vs 1/1; the Dec-31 versions are posted, the Jan-1 twins are NOT loaded and must stay excluded (or be CEO-confirmed) at the 2025 verbatim re-do. **Two production defects found + root-fixed in this PR:** (1) `ar_invoice_lines_compute_total_trg` silently re-totals any line inserted with BOTH quantity and unit_price_cents to GROSS (qty×price), clobbering the runner's discounted NET while the JE posts net — the runner now only sends unit_price_cents when it reproduces the net total (runner_version 3), the 2,953 clobbered 2024 lines were repaired (+$23,032.15 overshoot reversed, receipts re-posted); ⚠️ the **2025 load carries the same latent defect on 5,181 discounted invoices (documents $113,921.34 above their own JEs)** — part of the known 1107/1108 tie-out breaks, to be fixed by the 2025 verbatim line re-do. (2) **The receipts pipeline had NEVER posted a single JE** — `ar-receipts-reconcile` tagged receipts `source_table='ar_invoices'`, colliding with the invoice's own accrual JE under `uq_je_source_basis` (dup-key on every attempt); now posts under the provenance token `source_table='ar_receipts'` (the `ap_payment_import` convention). **5,331 receipt JEs posted** (DR 1030/1051, CR the invoice's AR class, posting_date=FullPaymentDate; 19 registry-open invoices $10,949.18 correctly remain unpaid). The 8/31/2024 opening balance sheet (opening inventory/equity) remains pending CEO exports. User-guide ch16.
> - **#1677 Factor customer links + AP AmountPaid delta watcher.** (1) **Factor links:** 6 of the 7 unlinked Rosenthal customer numbers in `factor_customers` matched to Tangerine customers with WHERE-guarded prod UPDATEs — Ocean Hut 405767→CUST-00097, Hilton Waterfront Beach Resort 704940→CUST-00064 Hilton TWBR, SWFM 786256→CUST-00154, Discount Fashion Warehouse/DFWH 680749→CUST-00032, Variety Wholesalers 236410→CUST-00175, Tiendas La Gran Via 244993→CUST-00161 (dupe-pair candidates resolved to the coded/active customer; the 2026-06-11 inactive uppercase twins have zero AR/SO references). Links rippled into `factor_ar_open_items` (36 NULL rows → 0) and `factor_chargebacks`. **MACYS CORPORATE SERVICES 577512 left unlinked for CEO review** — distinct from Macys, no matching record; its 5,777 chargeback rows (net −$638,258.67) are the whole remaining NULL set. Directory now 12/13 linked; the recon tie-out itself is unaffected (it proves cents, not links) — this enables customer-grain joins/drills. (2) **AP AmountPaid delta watcher** (nightly `/api/cron/ap-paid-delta-watcher`, 06:30 UTC): the go-forward guard for #1668 — Xoro keeps paying imported bills, and the live `bill/getbill` feed carries no AmountPaid amounts/dates/accounts (and skips register-frozen invoices), so the watcher runs in **register-comparison mode**: latest `ap_bill_register_import` + `ap_payment_import` staging vs posted state. Posts increments exactly like #1668 (payments: DR 2000/CR mapped account @ Paid Amount, `ap_payment_historical`, same JE key as the script; relief deltas: DR 2000 / CR 5005 / CR 1308, `ap_relief_historical`; SOURCE dates always, cutover-clamped) and re-aligns `invoices.paid_amount_cents` so the 06:00 tie-out proves fresh register state. Anomalies alert-only (bell+email admin+accounting + app_errors→digest): paid_decreased / total_changed / relief_decreased / new_bill / payment_unresolved / **vendor_cash_drift** (stateless register-vs-cash-vs-8002 invariant — re-alerts nightly until the Payments export lands). Baselines on staging (`paid_processed_cents`, `relief_5005/1308_processed_cents`, import-upsert-proof); run log `ap_paid_watcher_runs` feeds a new **Sync Health** `ap_paid_watcher` row with last-run counts. Migration `20260971000000` (idempotent; applied to prod + schema_migrations recorded). CLI `scripts/run-ap-paid-watcher.mjs [--dry-run]` (self-heals stale service key via Management API) for post-import triggering. **First prod run: 3,680 bills checked, 0 payments, 0 relief, 0 paid deltas, 0 anomalies; second run identical (idempotency proven); feed row ok.** User-guide ch13 (new watcher section) + ch16 (directory state).

> **Recent cross-cutting landings (2026-07-09)** — not tied to a single phase row:
> **Recent cross-cutting landings (2026-07-09/10)** — not tied to a single phase row:
> - **#1676 3-Way Match module (PO ↔ receipt ↔ AP bill)** — the classic apparel-ERP payables control, now bill-grain over the ENTIRE AP book. New engine `run_three_way_match()` (SQL RPC, set-based, idempotent — writes only `ap_bill_matches` verdicts, never GL/bills/POs): per bill it (a) finds POs via explicit `invoice_line_items.po_number` refs (the bills-register import carries them on merchandise bills) or a **fuzzy vendor+amount+date-window pass** (unique candidate only), (b) takes receiving evidence from `po_line_items.qty_received` (Xoro mirror) / `purchase_order_lines.qty_received` (native-only POs) — the standalone receipt tables are empty in prod, so PO-line received-qty IS the receipt leg, (c) computes price (bill avg unit price vs PO), qty (cumulative billed qty vs received) and amount (cumulative billed $ vs received $) variances against a **tolerance config table** (`ap_match_tolerances`: qty ±2%, price ±1% or $50, amount $100 — panel-editable). Statuses worst-wins: over_billed_vs_received / qty_variance / price_variance / no_po_found / matched_2way_po_only / matched_3way / not_applicable (vendor has no POs = expense/service bill). Nightly cron 06:45 UTC (after bank-mirror 05:30 + tie-out 06:00; `app_errors` source='cron'; notifies accounting on open over-billed). Panel: 3-Way Match gains a default **Bill Match Audit** tab (tiles by status with open-$ cut, filterable exception grid w/ seq-guard, per-PO evidence modal, View bill/PO drills, **Accept variance / Dispute with mandatory audit-logged reason** via `resolve_ap_bill_match` RPC + `set_audit_context` (T11); resolutions survive re-runs unless the verdict changes) — the original P13-C4 vendor-invoice staging flow lives on unchanged in a second tab. RBAC: `three-way-match` segment + `three_way_match` panel → `ap_invoices`. Migration `20260974000000`. Backfilled over all prod bills. User-guide ch32 §32.7.
> - **GL core-financial reports now return TRUE cents + Income Statement live date-refresh.** Two fixes, one PR. (1) The Income Statement panel only loaded once on mount (default FY window) and ignored subsequent From/To/basis changes — the grid kept stale rows while the xlsx export filename carried the *new* dates (a CEO export of "2025-01-01 to 2025-12-31" returned 2026-YTD data). Effect deps now `[basis, from, to]` (mirrors Balance Sheet; the existing seq-guard handles rapid-change races). (2) **Dollars-as-cents class fix** (retires the documented gotcha): `journal_entry_lines.debit/credit` are numeric **dollars**, but the P5 reports cast `SUM(...)::bigint` and labelled it `*_cents` — so every figure displayed **100× too small** *and* the whole-dollar truncation dropped the cents entirely ($9,947,831.51 → 9947831). Migration `20260970000000_gl_reports_true_cents` recreates **`v_trial_balance` / `trial_balance()` / `v_income_statement` / `income_statement()` / `v_balance_sheet` / `balance_sheet_as_of()` / `cash_flow_indirect()`** as `ROUND(SUM(<dollars>)*100)::bigint` (balance sheet + cash flow included because the BS panel combines `balance_sheet_as_of` with a sibling `income_statement` fetch for Current-Year Earnings — converting one alone would desync them). Every compensator updated in the same PR so nothing double-converts: `tieouts.js` `glNetCents` now `intCents` not `dollarsToCents` (the `runCashMirrorTieouts` path reads raw JE-line dollars → keeps `dollarsToCents`); `scripts/post-bills-register.mjs` `toC` no longer ×100 for its v_trial_balance read. Applied to prod + verified: **2025 net revenue $25,632,390.60, 2026 YTD $15,129,131.27, 2000 = $9,947,831.51 (cents recovered)**; subledger AP 2000 + all cash-mirror tie-outs tie to $0.00 (AR-backfill + factor-advance breaks pre-existing/expected). full suite 9786 green, typecheck 0 new, build + eslint green. User-guide ch38.
> - **#1669 Drill-through Phase 2 — agings, Segment P&L → GL, bank recon links** (continues #1657 Phase 1). (1) **Aging bucket drills:** every amount on AR Aging / AP Aging (cell, row total, column total) opens `AgingDrillModal` — the open invoices/bills behind that number via new complete-aggregate endpoints `GET /api/internal/ar-aging/detail` + `/ap-aging/detail` (?bucket [current|1-30|31-60|61-90|91-120|120+|total AR / 91+ AP] &as_of &customer_id/vendor_id; replicates the report SQL's bucket CASE exactly — **verified on prod: every bucket ties to the cent**, AR $40,768,113.51/22,978 open invoices, AP $9,947,831.51/123 bills, per-vendor cell $3,514,221.30 TIE). Rows link **↗ → AR/AP Invoices (?q)** + **JE → JEDetailModal** (accrual_je_id). Totals always cover the whole bucket; the row list caps at 5,000 (banner + `truncated` flag). Deep links `?m=ar_aging|ap_aging&bucket&party[&as_of]` (one-shot consume + replaceState sync). **Bonus fixes:** AR Aging panel read bucket fields neither API shape ever returned — every amount rendered "—" (now pivots long/wide like AP; 6 SQL buckets split 91-120/120+); view-mode party names now enriched server-side; view-mode limit now applies per party (was silently dropping buckets past 500 (party,bucket) rows). (2) **Segment P&L → GL:** Net Sales / per-gender / COGS cells (segments + Total) open `SegmentGLDrillModal` via `GET /api/internal/segment-pl/gl-drill` — maps the cell's dims through the SAME `resolveRevenueRouting` the Xoro bridge posts with (per-dims + `is_pl` = style_code ~* 'PL$' via new RPC `segment_pl_gl_drill`; ecom store-scoped) and lists each account: **sub-ledger share (ties to the cell to the cent — PL $141,463.23 / PT DTC $4,206.82 / Total $417,495.91 all TIE)** vs **GL posted net** (new RPC `gl_range_activity_by_code`, cents ×100; **verified = the gl_detail line sums exactly**, e.g. 4005 $258,948.94 / 4008 $4,204.40 for 07-01→07-07) + `shared|exclusive` coverage flag; ↗ → GLDetailModal → JE → source doc. (3) **Bank recon:** matched/auto-posted rows get a **JE <number>** button (handler resolves `matched_je_line_id` → JE header) and every row gets **GL ▸** → GLDetailModal on the txn's bank GL account ±7 days (unmatched = "find the counterpart"); `bank_transactions` list now embeds `gl_account_id`+`gl_accounts(code,name)`. Migration `20260969000000` (2 read-only STABLE RPCs, idempotent, pre-applied to prod for verification). RBAC: `ar-aging`/`ap-aging`/`segment-pl` segments → `analytics` (read+export, scorecards/factor precedent). 10 new vitest cases (bucket math mirrors SQL boundaries; routing map incl. shared-flag + '(none)' gender). typecheck 0 new + vite build + eslint green. User-guide ch03 (main section) + ch16/17/19 cross-refs.

> **Recent cross-cutting landings (2026-07-08)** — not tied to a single phase row:
> - **Factor Module Phase 1 — Rosenthal Capital Group** (apparel gap #1 groundwork; RE-RATE 2026-07-08 apparel gap = chargebacks). New tables `factor_statements` (monthly CLIENT RECAP economics — net sales / collections / chargebacks-net / commissions / interest / fees / advances / OAR rollforward / net due client / total loans, integer cents, unique per `statement_month`), `factor_ar_open_items` (month-end FACTORED AR DETAILED invoice grain, unique `(as_of_date, item_num)`; Σ `item_balance_cents` per as-of ties to the report footer Net OAR to the cent), `factor_customers` (Rosenthal number → `customers` link, six seeded: Bealls 111987 / Burlington 119432 / DD's 133867 / Island Leisure 676622 / Ross 211832 / Macy's Backstage 683407). RLS auth_internal only (20260964 posture, no anon). Importer `scripts/import-factor-pdfs.mjs` (python/pypdf text extraction; pure parser `api/_lib/factor/parseFactorPdfText.js` with 18 vitest fixtures — handles both filename vintages MM.YYYY + MM.YY, both letterheads (Rosenthal & Rosenthal / Rosenthal Capital Group), glued type suffixes `ROF-I014115I`, OAP deduction rows, signed chargebacks/fees; enforces per-statement OAR rollforward + month-chain continuity + footer tie-out; idempotent upserts). Panel **Tangerine → Accounting → Factor (Rosenthal)** (`factor_recon`): statement grid → month drill (open AR by customer with computed aging as of the report date) + **GL 1107 tie-out strip** via `/api/internal/trial-balance`. Handlers `/api/internal/factor/statements` + `/api/internal/factor/open-items` (authenticateInternalCaller; `factor` RBAC segment → analytics). Loaded: 12 statements (2024-10 → 2025-09, chain continuous) + 3 AR snapshots (7/31 $3,765,796.32 · 8/29 $3,686,303.87 · 9/30 $3,674,972.73 — all tie). Phase 2 = factoring-cost JEs + chargeback dispute tracking. User-guide ch16.
> **Recent cross-cutting landings (2026-07-09)** — not tied to a single phase row:
> - **Factor Module Phase 2 — chargeback disputes + factoring-cost JEs** (completes the Phase-1 deferred scope). New `factor_chargebacks` (item grain from the Rosenthal "Chargeback Report" Charge Back Analysis section; signed amounts — positive = chargeback, negative = creditback; best-effort reasons + 3-digit codes from the CHARGEBACK/CREDITBACK SUMMARY, merged at date grain by Rosenthal so not every item resolves; unique `(report_month, item_num, cb_date, batch, amount_cents, dup_seq)`; dispute columns `status`/`notes`/`status_history` operator-owned — the importer never touches them). Loaded **11 months Jul-25 → Jun-26, 5,928 items; every month's Σ ties to the report TradeStyle Total = −(recap chargeback line)** (Jul/Aug/Sep cross-checked against factor_statements); Feb-26 report not provided by Rosenthal, no detail exists before Jul-25. AR snapshots extended to **12 as-ofs (2025-07-31 → 2026-06-30, all tie to the footer to the cent)** — parser hardened for the 2026 vintages ("Net 10+75 E" terms, "SR-"/lowercase/dashless type-O deduction refs, type "RI" re-invoices, batchless chargeback rows, sign-flipped page-3 facility totals). **Factoring-cost JEs:** `scripts/post-factor-cost-jes.mjs` posts one accrual JE per statement month — DR 6802 Factor Commissions / DR 6804 Factor Interest (TOTAL INTEREST + prior-month adj) / DR 6803 facility FEES+OTHER only (the recap "(FACILITY)" total embeds the PRIOR month's interest → posting it whole would double-count) / CR 1051 Factor Advances — month-END posting dates (source dates), T11 audit reasons, idempotent per month (`source_module='factor_recap'`, `source_id=<statement_month>`, `uq_je_source_basis` DB backstop). **12 JEs posted 2024-10 → 2025-09, total $515,690.72** (commissions $141.7K + interest $373.1K + fees $0.9K); Oct-24's anomalous −$188,930.78 facility OTHER (a one-off loan credit, not a fee) is EXCLUDED from 6803 pending CEO/Rosenthal clarification. **Chargebacks tab** on the Factor (Rosenthal) panel: per-customer rollups, inline status dropdown + notes (PATCH `/api/internal/factor/chargebacks/:id`, status_history audit), month/status filters, xlsx export. `factor` RBAC segment moved analytics → finance_misc so the dispute write is enforceable. CLIENT RECAP statements Oct-25 → Jun-26 still pending from the CEO — cost JEs stop at Sep-25 until they arrive. Migration `20260967000000` (applied manually + schema_migrations recorded; 20260966 was claimed by #1668 mid-flight). Parser 26 fixture tests. User-guide ch16.
> - **#1671 Bank reconciliation mirror — GL cash proven against Xoro's reconciled bank activity.** All bank accounts are reconciled IN XORO through **2026-05-31** (Plaid configured, not live), and Xoro's REST API exposes NO bank/deposit/payment/reconciliation endpoint under any private-app scope we hold (probed 2026-07-08: ~30 path variants × 4 credential pairs; only `purchaseorder/*`, `bill/getbill`, `bill/getitemreceipt` respond — `bill/getbill` carries only header AmountPaid, no payment grain). So the mirror derives from the **Payments-register staging** (`ap_payment_import`, #1668): 13 `bank_accounts` created (Valley 1001 ····7801 / 1002 ····1300 / 1003 ····1500 — "Bank Leumi" register names map to VALLEY GL accounts, never their own; CCs 2101–2108 AmEx+Chase; Rosenthal factor facility 1051 tagged `factor_settlement`; Cash Clearing 1020; masks last-4 only), **2,551 `bank_transactions`** (`source='xoro_mirror'`, `external_txn_id`=payment number, SOURCE dates; 133 zero-cash docs skipped; 1 excluded → Opening Balance Equity B001023 $65,790.35), **100% auto-matched** (pass 1 register→JE linkage, confidence 100; ±3d amount/date fallback ready for AR receipts), **312 `bank_recon_runs`** (`source='xoro_mirror'`; **285/286 closed account-months reconciled to the cent**, 26 open in_progress). The single flag: 1001 May-2026 off **−$10,010.00** = two operator TEST JEs dated 2026-05-31 (JE-2026-00001 "rest" $10,000 + JE-2026-00002 "post po issue" $10) — books-only, recommend reversal. **Honest caveat: register = payments only; AR receipts exist in NEITHER the mirror NOR the GL (`ar_receipts` empty), so GL cash = −$31.0M on 1001 is outflow-only, not a real bank balance — the mirror proves GL⇄register agreement until deposits land (AR backfill or Plaid).** Nightly `/api/cron/bank-mirror-sync` (05:30 UTC) re-syncs/re-matches/rolls open months; the 06:00 subledger tie-out now emails a **CASH mirror tie-out** section (GL vs mirror per account as of 2026-05-31; runCashMirrorTieouts in `tieouts.js`, ACCRUAL as-of, normal-side signed). Plaid seam: normalized ingestion contract `api/_lib/bank-feeds/ingest.js` (+5 vitest) — go-live needs env vars only (PLAID_CLIENT_ID/SECRET/ENV/TOKEN_ENC_KEY), no schema change; P9 cash-engine now classes `xoro_mirror` on the Xoro side. Files: `api/_lib/bank-mirror/mirror.js`, `scripts/import-xoro-bank-history.mjs` (idempotent, `--report`), `api/_handlers/cron/bank-mirror-sync.js`, migration `20260969000000` (source CHECKs + `bank_recon_runs.source`). NO JEs posted — mirror + report only. User-guide ch17.
> - **#1668 AP bill-history backfill — 2000 proven against the CEO's Bills register to the cent.** The full Xoro AP history posted to the GL from the Bills register export (**3,680 bills, Oct-2023→Jul-2026, $51,080,712.61**) + the held Payments staging (**2,685 payments, $41.13M**): 3,534 accrual JEs (`ap_invoice_historical`; DR 1201 for Suppliers/Manufacturer bills, else vendor default expense/8007; CR 2000 vendor-subledgered; 145 deduped vs the #1662 sweep), 24 register true-up deltas (net −$57,445.72), 257 relief JEs (discounts+vendor credits → 5005, prepayments applied → **new 1308 Vendor Prepayments & Deposits**), 2,552 cash payment JEs (`ap_payment_historical`, DR 2000 / CR mapped account @ Paid Amount — the exports prove Σ(Amount−Paid) ≡ Σ bill relief **to the cent**, so 133 zero-cash payment docs correctly post nothing), 1 Porsche residual ($6,236.32 → 8002), 1 control alignment (mfg service credits reclassed 2000→2160; dry-run reversal pair neutralized). Pre-cutover bills (58, all paid) post at the 2024-08-31 opening date — no opening 2000 JE exists, so the dsantiago 08/31/2024 opening-AP bills accrue as the opening balance. Register bills freeze as `invoices.source='xoro_bills_register'` (nightly sync skips them). **End state: GL 2000 $9,947,831.51 CR = register Σ Amount Due, diff $0.00; the #1665 tie-out's `pending_payments` waiver lifts and AP reports ok.** Files: `scripts/import-bills-register.mjs`, `scripts/post-bills-register.mjs` (reconcile→link-invoices→accruals→deltas→relief→payments→residuals→verify, all idempotent via `uq_je_source_basis`), migration `20260966000000` (staging table + `xoro_bills_register` source + GL 1308). User-guide ch13 "Historical bill backfill". ⚠️ Post-export Xoro activity is NOT reflected — re-run from fresh exports at cutover.
> - **#1665 Daily subledger → control-account tie-out monitor** — from the 2026-07-08 accounting audit ("best-in-class books are continuously proven"). New cron `/api/cron/subledger-tieout` (06:00 UTC daily, `vercel.json`) compares each control account's **cumulative posted-ACCRUAL GL balance** (via `v_trial_balance` — SUM of `journal_entry_lines` debit−credit over posted JEs) against its **subledger total**: AR classes **1105 CC / 1107 factored / 1108 house** vs open `ar_invoices` (`total_amount_cents − paid_amount_cents` grouped by `ar_account_id`; draft/pending/void/reversed excluded), and AP **2000** vs unpaid `invoices` (vendor bills) with `gl_status='posted'`. Any **|diff| > $0.01** → ONE bell+email (roles admin+accounting, `kind='subledger_tieout_break'`, same notification path as the Xoro feed-health alert) **plus** a `captureError` row so the daily app-errors digest catches it too. **Expected-diff handling:** while the AR history backfill is mid-flight, AR breaks are expected (the alert going quiet = backfill done); AP 2000 is **waived as `pending_payments`** (reported in the JSON `waived` field, NOT alerted) while `sum(paid_amount_cents)=0` across posted bills — the waiver lifts automatically when the first payment lands in the bills ledger. Pure, unit-tested engine `api/_lib/accounting/tieouts.js` (`buildTieoutRows` / `runControlTieouts`, sign-aware: AR net-debit, AP net-credit; 21 vitest cases incl. a chainable supabase double). Files: `api/_lib/accounting/tieouts.js` (+tests), `api/_handlers/cron/subledger-tieout.js`, routes manifest + `gen:routes`, `vercel.json`. No migration. check:api + 7,185 api tests + typecheck 0 new. User-guide ch03.

> **Recent cross-cutting landings (2026-07-02)** — not tied to a single phase row:
> - **Left nav drawer: right-click a menu entry → open that view in a new tab** — every entry in the live navigation drawer (`src/tanda/NavDrawer.tsx`) — Favorites rows, search-result rows, and module rows — now handles **`onContextMenu` (right-click)**: it `preventDefault`s the native browser menu, builds the deep-link (`?m=<module_key>`, resolved to an absolute URL against the current location), and opens it with `window.open(url, "_blank")` then `win.focus()` so the new tab is brought to front. **Left-click is unchanged** (in-place `onSelectModule` navigation; existing middle-click / Cmd-click still fall through to the browser). Each row carries a `title="Right-click: open in new tab"` tooltip. No deep-link work was needed — Tangerine already selects `activeModule` from `?m=` on mount (`src/Tangerine.tsx`), so the opened tab lands directly on that panel. Single-file change (+ AI user-guide snapshot regen). Typecheck ratchet 0 new + vite build green. User-guide ch01.
> - **#1638 PO modal matrix: render the sizes the order actually uses (off-scale + off-grain)** — a PO (ROF-P001265) rendered a **blank** size matrix for both `RBB0185` and `RBB0185-03SFPPK` even though the grid total was correct ($215,033.76 ties out). Two causes, one root: the modal considered only the style's *declared* sizes, not the sizes the order actually carries. (1) `RBB0185`'s size scale is **"Even Num Waist 28-42"** but the PO ordered **kids sizes 8/10/12/14** — the columns rendered (via the #1631 off-scale fix) but the locked-row **visibility filter** still used only the scale sizes, so every row (qty all off-scale) filtered away → blank. Fix: the filter (`entrySizes`) now uses the effective sizes (scale ∪ actually-ordered). (2) `RBB0185-03SFPPK` has a prepack definition so it rendered a single **PPK48** column, but the order was cut at garment sizes 10/12/14 → the pack column had no qty. Fix: a new `ppActive` gate renders a style as a **prepack only when the order is at pack grain** (nothing entered yet, or a line at the pack token); a prepack-defined style ordered per size now renders as a normal size matrix. Applied to both the on-screen matrix AND `getDocumentData` (View → PDF/Excel). File: `src/tanda/LineMatrixBody.tsx`. No migration. Typecheck 0 new + build green + **9,672 tests pass**. User-guide ch28.

> - **#1637 PO grid Phase 4: ingest grain guardrail** — stops the grain-less-PPK-line class from re-entering at import (the class Phase 1 had to backfill). In `import-xoro-orders.mjs` `resolveSku`: when a PPK line finds **no sized pack row** but a **color-only** master row (size null — the "color-only rows stale" gap) exists for its colour, it now **binds to that row AND stamps the pack size** from the ItemNumber's `-PPKnn` token (guarded — never overwrites an existing sibling at that size). So a fresh import of a prepack style whose master carries only color-grain rows lands **grain-bearing** (per-each) instead of grain-less (which the grid read at pack grain, e.g. $127/each). Also: the `--apply` PO summary now prints an **ingest-guardrail nudge** to run `npm run audit:pos` after every import. Files: `scripts/import-xoro-orders.mjs`. No migration. `node --check` green. User-guide ch28.

> - **#1636 PO grid Phase 3: CI regression net + size-sort consolidation** — turns "green today" into "stays green". (1) **`npm run audit:pos`** — the read-only PO-grid audit is now a first-class command (schedule it alongside the nightly agents to catch DATA drift; exits non-zero on any invariant failure). (2) **CI logic gate** — new `api/_handlers/internal/purchase-orders/__tests__/grain-invariants.test.js` runs the audit's invariants (cost/PO-price ratio ∈ [0.5,2], per-each plausibility, sane margin) over a **fixture of the exact prod shapes that broke** (P001177 pure-PPK, P001068 mixed garment+PPK, P001133 brand-list PPK, P001271 newly-linked) — a grain regression now **fails the build** (runs in the standard `vitest` CI). (3) **Size-sort consolidation** — `utils/tandaTypes.sizeSort` now **delegates to the shared `src/shared/sizeSort.ts` `compareSizes`**, so the PO WIP matrix + exports + row-detail all order sizes through ONE comparator (folds in the kids age-range fix from #1634); made numeric-first to preserve the frontend's tested convention. Verified against the **entire** suite: 9,672 tests pass. No migration. Typecheck 0 new + build green. User-guide ch28.

> - **#1635 PO grid Phase 1+2: unlinked-line backfill script + grain consolidation onto shared resolver** — closes the audit's remaining money/data reds. **Phase 1 (data, applied):** `scripts/backfills/link-ppk-prepack-lines.sql` (idempotent, committed for the record) linked the 6 unlinked POs' 21 SKU-less PPK lines to their catalog SKUs (positional Xoro-item match → unique normalized sku_code) AND set the pack **size** on the resolved color-only rows (from the Xoro `-PPKnn` token, collision-guarded) so grain resolves per-each — P001271 $127.20/pack → **$5.30/each**, not pack-grain. Audit ALL/SOME_UNLINKED 6→0. **Phase 2 (code, the real root):** `computePoLineMoney` now delegates the ambiguous-cost-grain decision to the **shared `resolvePerUnitCost` (`api/_lib/sales-grain.js`)** — the SAME resolver the sales COGS path already uses (`master cost > 2× the per-unit reference ⇒ per-pack ÷ pack size`). Purchasing + sales now share **ONE grain resolver** instead of two divergent implementations (the anchor was a reinvention). Behavior-equivalent (10/10 tests unchanged); `packGrain` verdict still carried to SELL so cost + sell agree. Files: `api/_handlers/internal/purchase-orders/index.js`, `scripts/backfills/link-ppk-prepack-lines.sql` (new). No migration. Typecheck 0 new + build green + 10/10 tests. User-guide ch28.

> - **#1634 PO grid audit tool + canonical size-sort + prepack-label fix (Phase 0/B of the PO-grid audit)** — after ~30h of one-PO-at-a-time patching, shifted to **systematic audit**. (1) **`scripts/audit-pos.mjs`** — a read-only tool that runs all active POs through invariants (totals tie out, cost/grain sanity via the anchor, per-each plausibility, style-code case, description present, all/some-unlinked, PPK-without-prepack-def, unrecognized size) and prints a categorized failure report — the standing "prove it's zero" check. First run over 123 POs: **money math clean (totals/grain/case/plausibility all 0)**; remaining = 5 all-unlinked + 1 partial, 11 POs with PPK styles lacking prepack defs, 4 POs with kids age-range sizes. (2) **Canonical size sort** — new `src/shared/sizeSort.ts` (`compareSizes`, +5 tests) mirrors the backend `sizeSortKey`; handles kids **age-range** sizes (`XS(5-6)`, `S(7-8)`, `M(10-12)`) that a local `SIZE_RANK` map alpha-sorted into L,M,S,XL,XS. Row-detail now uses it. (3) **Prepack label** — the row-detail no longer shows "packs / PPK exploded" on **non-prepack** styles (CEO: "all carrots say prepack exploded"); shows "Qty" / "Unit $" unless the PO actually has PPK lines. Files: `scripts/audit-pos.mjs` (new), `src/shared/sizeSort.ts` (+test), `src/tanda/InternalPurchaseOrders.tsx`. No migration. Typecheck 0 new + build green + 5/5 tests. User-guide ch28.

> - **#1632 Canonical style_code on SKU creation (mis-cased REST-import codes stranded from their style)** — a PO (ROF-P000265) errored **"Style rbb0185 not found"** even though the style exists as **RBB0185** — because 37 `ip_item_master` rows carried the **lowercase** `rbb0185`, and `style_master` lookups are **case-sensitive**, so the size matrix couldn't resolve it. Root: a REST/import feed's mis-cased `BasePartNumber` was inherited verbatim onto new SKUs. Fix: `resolveOrCreateSku` (`api/_lib/styleMatrix.js`, the shared SKU-creation point for the ingest, build-orders, and the matrix resolve-sku endpoint) now takes the **canonical `style_code` from `style_master` by `style_id`** (authoritative) instead of the caller's string — a mis-cased feed code can no longer strand a SKU from its style. **Data patch (applied):** corrected **143** existing `ip_item_master` rows across **28** style codes (rbb0185, ptya0009, cxt0002L, PTYG0003lstd…) to their canonical `style_master` casing (verified 0 case-collisions first, so unambiguous). No migration. Typecheck 0 new + build green + `node --check`. User-guide ch28.

> - **PO grid: reference-grain ANCHOR (fixes mixed garment+PPK cost/sell) + on-screen off-scale size** — mixed styles (RYO0659FP jacket = garment sizes SML..XLG **and** a PPK18 pack) showed **Avg cost $0.66 / Sell $0.86 / Margin −1278%** and a **blank** on-screen matrix (though Total $87,239.70 was right). Two fixes: (1) **grid** — `ip_item_avg_cost` (+ sell tables) store PPK costs at an **inconsistent grain**: per-PACK for a *pure* prepack style (RCB1869NBDPPK = $324) but per-EACH for a *mixed* style (RYO0659FP = $11.85 = $213.30/18). With no grain tag, `computePoLineMoney` now **anchors** to the line's own per-each cost (`poPrice ÷ ppk`, grain known): whichever of {std, std÷ppk} is closer is the per-each std, and that grain decision is applied to SELL too (no std → structural fallback on a `PPK` style_code). Fixes **both** directions — pure-PPK $324→$5.40 (was reading $324/each) and mixed $11.85 stays $11.85 (was $0.66). Result: P001068/P001163 Avg cost **$11.85**, Sell **$15.50**, margin ~23%. (2) **on-screen** — `LineMatrixBody` size columns now include any **ordered size the scale doesn't carry** (the PPK18 token), so the modal matrix shows the packs instead of a blank grid (mirrors the #1630 document fix; prepack-defined styles use the pack column anyway). Files: `api/_handlers/internal/purchase-orders/index.js` (+`__tests__/po-line-money.test.js`, now **10 tests**), `src/tanda/LineMatrixBody.tsx`. No migration. Typecheck 0 new + build green + 10/10 tests + live-prod SQL. User-guide ch28.

> - **PO document (View → PDF/Excel): off-scale PPK pack size no longer drops the qty** — two POs (ROF-P001163 / -P001068) exported with **Qty 0 / $0** although the grid showed them correct (~$85–87k). Cause: the styles (RYO0659FP jackets) carry **both garment sizes** (SML/MED/LRG/XLG) **and a `PPK18` pack size**, and had **no prepack definition**, so in `LineMatrixBody.getDocumentData` the size list was `payload.sizes.filter(sizesSeen)` — the intersection of the garment scale with the *ordered* PPK18 was **empty**, so no size column rendered and the packs' qty was dropped from the doc. Fix: keep the scale sizes that were ordered **plus any ordered size the scale doesn't carry** (the PPK pack token), so a sized style with prepack lines still exports its quantities. (Once a style gets a prepack definition — Style Master → Prepack matrix — `pp.pack_token` drives the column anyway; this is the safety net for styles without one.) Client-only, no API. File: `src/tanda/LineMatrixBody.tsx`. Typecheck 0 new + vite build green. User-guide ch28.

> - **PO grid: Sell grain fix (brand/customer price weighted by qty, not eaches) + 1 bad-data row** — a prepack-heavy PO (ROF-P001133) showed **Sell $65.66 / Margin 89%**. Two causes: (1) **code** — `computePoLineMoney` weighted the **brand-list / customer** sell by *eaches* while every other source weighted by *qty*; but a PPK (pack) style's brand list is **per-PACK** (the $390 list IS the pack price, like its $324 pack cost), so it read as **$390/each**. Every sell source is keyed by the line's style/SKU, whose grain matches the line's, so **all** sources now weight by **qty** (÷ eaches → per-each) — no per-source exception. Fixes every PO carrying a priced PPK style (105 PPK brand-list rows at ~$390). (2) **data** — exactly one **loose** style, **RYB1416**, carried a pack price in its per-each sell (brand list **$222.90**, provisional **$217.22**); recovered to per-each ÷24 (**$9.29 / $9.05**). It's the *only* loose style with pack-grain sell data (verified). Result: P001133 Sell **$65.66 → $9.11**, margin ~21%. Files: `api/_handlers/internal/purchase-orders/index.js` (+`__tests__/po-line-money.test.js`, now **8 tests**). No migration (1-row prod data patch applied). Typecheck 0 new + build green + 8/8 tests + live-prod SQL. User-guide ch28.

> - **PO row-detail: Issued / Received / Open breakdown toggle** — the ▸ row-detail matrix gains a **⊞ Issued / Received / Open** show/hide button (next to the Remaining/Original switch; shown when the PO has receipts). On, each colour renders **three stacked lines** per style/colour — **Issued** (original PO ordered qty, muted), **Received** (blue), **Open** (remaining to ship = ordered − received, teal) — each with per-size cells, per-metric row + style totals, and Ext $ (ordered/received/open value). Off = the single Remaining/Original line. Reuses one per-cell accumulation (`{ordered, received, remaining}` + their $) so all views render from the same data; honours EXPLODE PPK. Client-only. File: `src/tanda/InternalPurchaseOrders.tsx`. Typecheck ratchet 0 new + vite build green. User-guide ch28.

> - **PPK prepack lines: link at import + backfill (the "Other lines" root cause)** — a PPK PO exported as CSV showed the prepacks as flat **"Other lines"** (`LAIDBACK Baggy Fit … 178 @ $171.60`) separate from the style's size matrix, and the grid mis-costed them. **Root cause**: the Xoro ItemNumber for a prepack is `RYB153330PPK-Seaweed - Dark Wash-**PPK24**`, but the catalog pack SKU (`ip_item_master`) keeps the pack size in its **`size` column** and OMITS it from `sku_code` (`RYB153330PPK-SEAWEED-DARKWASH`, size `PPK24`). The import's `resolveSku` loose/expanded keys therefore missed by exactly the trailing `PPK24`, and step 3.5 explicitly skips PPK — so every prepack line imported **null-linked** (no style/size → can't join the matrix, excluded from Avg cost/PO Price). **Fix**: (1) `resolveSku` gains **step 3c** — for a PPK line, retry the loose/expanded match with the pack-size segment stripped, restricted to a family row whose own `size` is a PPK token (never binds a pack line to a loose SKU). (2) **One-time prod backfill** re-linked the **32** existing orphan prepack lines on active POs (matched by *position*: native line order ↔ Xoro item order — validated 53/53 orphans agree on qty+description, 0 mismatches — then unique normalized sku_code; sets only `inventory_item_id`, line ids/receipts preserved). ROF-P001133: 9 "Other lines" → their `RYB1533PPK`/`RYB1619PPK`/… pack SKUs (size PPK24), 0 orphans left, Avg PO Price now a clean **$7.15/each**, Total unchanged $243,336.90. Once linked, the shipped per-each grain fix (#1625) costs them correctly and they render as matrix blocks. **21** other orphans remain — their styles have **no pack SKU in the catalog** (RYB1257PPK, RYB1630BDPPK, RYO08xx…) → operator catalog task to create those PPK SKUs. Files: `scripts/import-xoro-orders.mjs`. No migration. `node --check` green. User-guide ch28.

> - **PO grid: PPK grain bug fixed at the root (per-each normalization in one tested helper)** — the recurring "PPK styles show ~98% margins / huge Avg cost" defect is fixed properly, not patched again. **Root cause**: every money column (Avg PO Price / Avg cost / Sell) is a `Σ dollars ÷ Σ eaches` **per-each** average, but a **PPK line** stores qty=**packs** + cost=**per-pack**, and the reference tables (`ip_item_avg_cost`, `recent_sell_by_style`) store PPK values **per-pack too** (a PPK60 pack's avg_cost *is* $324). Prior fixes normalised the *line-price* side (explode to eaches) but multiplied the *reference* cost/sell by **eaches** — so a $324/pack cost read as **$324/each** (×60) and Sell/Margin blended two grains (ROF-P001177: Avg cost $298.89, Sell $374.21, Margin 98.5% — all wrong; live SQL confirmed old=buggy, new=$5.55/$5.55/$6.95/~20%). **Fix**: extracted a pure, exported, **7-test** helper `computePoLineMoney(line, refs)` (`purchase-orders/index.js` + `__tests__/po-line-money.test.js`) that encodes the canonical grain rule **once** — native-grain (per-pack) cost + sales-history/std/provisional sell weight by **qty**, garment-list (customer/brand) sell weights by **eaches**, all ÷ eaches. `enrichPricing` now calls it per line instead of open-coding the (drift-prone) weighting. Non-PPK POs unchanged (ppk=1). No migration. Files: `api/_handlers/internal/purchase-orders/index.js`, `+__tests__/po-line-money.test.js`. Typecheck 0 new + vite build green + 7/7 tests + `node --check` + live-prod SQL. User-guide ch28.

> - **PO grid: Avg cost PO-price fallback + Avg PO Price reordered first + row-detail unlinked-lines panel + Remaining/Original toggle** — four fixes to the native Purchase Orders grid, all off the CEO's read of the live grid. (1) **Avg cost fallback**: `enrichPricing` (`purchase-orders/index.js`) now costs each **SKU-linked** line at its **standard cost when the SKU resolves one, else the line's own PO price** — so a PO whose colorway isn't in `ip_item_avg_cost` yet (e.g. RYB1619 **SANDLOT/MELTFIELD**, whose per-unit cost is a clean $7.15 the cost table only carries under other washes) **no longer shows a blank Avg cost**; `stdDen` now equals `priceDen` for linked lines, so Avg cost is populated wherever Avg PO Price is. Fully-**unlinked** POs (Ricowell-style description-only lines) still read `—` for both (no style to price). (2) **Column order**: **Avg PO Price** now renders **immediately before Avg cost** (header, body, totals, export) — price-you-pay left of the standard benchmark. (3) **Row-detail (▸) unlinked panel**: only lines with a real **style + size** build the color×size matrix; SKU-less **pack/aggregate** lines (e.g. ROF-P001135's 6 PPK-prepack rows @ $171.60 = $7.15×24) are shown in a separate **"Unlinked lines"** list instead of scattering as broken single-cell rows. (4) **Remaining / Original toggle**: a partially-received PO's ▸ matrix defaults to **remaining-to-ship** qty (`ordered − received`, floored) with a switch to **original** ordered — mirroring the **PO WIP** matrix's `itemQty` logic; ties the matrix to the grid's Remaining-to-ship column. No migration. Files: `api/_handlers/internal/purchase-orders/index.js`, `src/tanda/InternalPurchaseOrders.tsx`. Typecheck ratchet 0 new + vite build green + `node --check`. User-guide ch28.

> - **Inventory Matrix: SO-style empty-size-column collapse + Hide sizes toggle** — the read-only **Inventory Matrix** now reuses the exact collapse behavior of the **SO/PO size grids**. (1) The per-cell collapse logic in `EditableSizeMatrix` was extracted into a pure, shared **`computeSizeCollapse`** helper (`src/shared/matrix/sizeCollapse.ts`, +4 unit tests) — `EditableSizeMatrix` now calls it (SO/PO behavior byte-identical, existing tests green). (2) The single-style Inventory Matrix consumes the same helper: once a style has stock the **first size column with quantity turns green and is clickable**, hiding the all-zero **leading/trailing** size columns (mid-range zeros stay; `⋯` markers; click again to expand) — same green (`#10B981`), same collapse as the SO grid. (3) New **Hide sizes** toggle (near Hide 0s / By Inseam) drops **all** per-size columns, leaving Color / Total / Avg Cost / Total Cost / Last Received; honored in both the single-style and multi-style-block matrix renderers. The PO line body already carried the SO collapse (`collapsibleSizes={mode==="so"||mode==="po"}`). No migration. Files: `src/shared/matrix/sizeCollapse.ts` (new), `src/shared/matrix/EditableSizeMatrix.tsx`, `src/tanda/InternalInventoryMatrix.tsx`. Typecheck ratchet 0 new + vite build green. User-guide ch11.

> - **Inventory Snapshot: Avg Mrgn % column** — the Snapshot view (`InternalInventoryMatrix.tsx`) gains an **Avg Mrgn %** column placed **immediately before Avg Sale**, showing each row's gross margin = **(Avg Sale − Avrg Cost) ÷ Avg Sale** as a percent to two decimals (e.g. `42.15%`). Reuses the row's existing `sale_price_cents` + `avg_cost_cents` (no new data source); blank (`—`) when the row has no avg sale price (divide-by-zero guard) or no avg cost. Sortable (rows without a margin sort low), toggleable via **Columns**, and included in the **Totals** strip (an **Avg Mrgn** line per column from that column's avg sale/cost) and the **Excel export** (a dedicated percent column plus an **AVG — Margin %** totals row). Client-only, no API change. File: `src/tanda/InternalInventoryMatrix.tsx`. Typecheck ratchet 0 new + vite build green. User-guide ch11 + ch28.
>
> **Recent cross-cutting landings (2026-07-01)** — not tied to a single phase row:
> - **PO "View" button → PDF / Excel dropdown (xlsx export)** — the native Purchase Order modal's footer **View** button is now a **dropdown** (caret ▾, app dark palette) offering **PDF** and **Excel**. **PDF** is the existing branded printable document (unchanged; opens straight into the browser Print / Save-as-PDF dialog). **Excel** downloads the PO as an **`.xlsx`** via the shared **`downloadOrderExcel`** helper in `orderDocument.ts` — the same branded ATS-look renderer the Sales Order "Confirmation → Download to Excel" already uses, so PO and SO spreadsheets share one layout (logo banner, header block, per-style color×size matrix, prepack detail, grand totals). Refactored the PO's inline `openView` doc-build into a shared `buildPoDoc()` so PDF and Excel render from the identical `OrderDocument` and never diverge. Works on a draft too. No migration. File: `src/tanda/InternalPurchaseOrders.tsx` (reuses `orderDocument.ts`). Typecheck ratchet 0 new + vite build green. User-guide ch28.
> - **PO grid: default status + underscore-free labels + auto PO#, and a money-columns coverage BUG fixed** — (1) the PO grid **status filter now defaults to draft/issued/in-transit** (the live set) instead of All; (2) statuses render **without the underscore** (`in_transit` → *in transit*) in the filter options, grid chip and modal (raw value unchanged for filtering/API) via a `statusLabel()` helper; (3) the PO modal's **"PO number prefix" field is removed** — the number is always auto-assigned `PO-<year>-NNNNN` at issue (`nextPoNumber` already falls back to `PO`), so `po_prefix` is no longer sent. (4) ⭐ **BUG**: `enrichPricing`'s `purchase_order_lines` fetch used `.in(poIds-chunk-of-200)` with **no pagination** → PostgREST's silent **1000-row cap** dropped every line past row 1000 (prod runs ~68 lines/PO → a 200-PO chunk ≈13.6k lines), so **Avg PO Price / Sell / Margin were blank on most rows** though the PO carried the data (confirmed in prod: ROF-P001237 = 4 costed lines, grid blank). Fixed by paging each chunk with `.range()` (1000/page, stable `.order`) until exhausted. Avg cost `—` where no standard-cost history exists is a genuine data gap, not this bug. Files: `src/tanda/InternalPurchaseOrders.tsx`, `api/_handlers/internal/purchase-orders/index.js`. No migration. Typecheck 0 new + vite build green + `node --check`. User-guide ch28.
> - **SO missing-price warning on save** — if a style/color that carries a quantity has a **$0 Unit $**, `save()` now pops a **"Missing unit prices"** `confirmDialog` (listing each *style — color* via `listItems`) before resolving/saving. Warning, not a block: **Save anyway** proceeds ($0 is sometimes intended), **Add price** cancels. Detected from `bodyRef.getDocumentData()` (pure — no SKU resolve side-effects), so it runs ahead of the resolve/allocate path; covers matrix rows + flat lines, for both draft and confirm saves. File: `src/tanda/InternalSalesOrders.tsx`. No migration. Typecheck ratchet 0 new + vite build green. User-guide ch27.
> - **SO/PO matrix fixes: per-color line PO + auto-width qty cells + SO status-filter race** — three operator fixes. (1) The SO per-line Customer PO (from the split-into-new-SOs feature) moves from **per style section → per color ROW**: `EditableSizeMatrix` gains an opt-in **Customer PO** column (mirrors the `lot` column — per-row inputs + *set all* header, amber border when a row differs from the header PO), and `LineMatrixBody` stores `poByRow` per section and stamps `ResolvedLine.customer_po` per color row (was per section); the old per-section field (which was `disabled` on a locked SO) is removed, so the PO is now **always editable**. Split grouping in `commitSave` is unchanged (groups by `customer_po`). (2) The **qty cells auto-widen** to the digits entered (`width: max(5, len+2)ch`) so 4-5 digit quantities aren't clipped — SO **and** PO bodies (shared component). (3) **SO grid status-filter race** fixed: the multi-select fires one `load()` per checkbox toggle; a slower earlier response could land last and clobber the newest filter (e.g. "cancelled only" flashing then reverting). Added a `loadSeqRef` sequence guard — only the latest request's result is applied. No migration. Files: `src/shared/matrix/EditableSizeMatrix.tsx`, `src/tanda/LineMatrixBody.tsx`, `src/tanda/InternalSalesOrders.tsx`. Typecheck ratchet 0 new + vite build green. User-guide ch27.
> - **PO Cancel + Reinstate (SO parity)** — the native Purchase Order modal footer gains a red **Cancel PO** (issued/in-transit → `cancelled`, kept for history) and, on a cancelled PO, a green **Reinstate** (→ `issued`, keeps its PO #), mirroring the Sales Order Cancel/Reinstate pair. Closes a gap: the backend supported `PATCH {status:'cancelled'}` (closes P13 open-PO commitments) but no button existed. **Reinstate is commitment-aware** — the `[id].js` issue branch now re-opens the `po_commitments` the cancel closed (`cancelled → open`, `closed_at` cleared) when `po.status==='cancelled'`, so the D3 off-balance-sheet open-PO commitment reflects the live PO again (partially-received edge returns to `open` not `partial`). No migration. Files: `src/tanda/InternalPurchaseOrders.tsx`, `api/_handlers/internal/purchase-orders/[id].js`. Typecheck ratchet 0 new. User-guide ch28.
> - **SO per-line Customer PO → auto-split into separate confirmed SOs** — the Sales Order line matrix gains a **Show line PO** toggle (next to *Show lots*/*Show images*) that reveals a **Customer PO** field on every **style line**, pre-filled with the header PO. Editing a line to a **different** PO flags it (`↳ splits to a new SO on save`); on save, each distinct new PO **auto-creates a new sales order** copying **all** of the source order's header info (customer, ship-to, brand, channel, dates, terms, buyer, warehouse, fulfillment, factor), carrying only the line(s) that share that PO (same-PO lines grouped onto one SO), **auto-confirmed** (SO number assigned), with the note **"Auto created due to new Customer PO."** The source order keeps the header PO + matching lines (blocked if *every* line is reassigned). A **summary dialog** lists the new SOs (number · PO · styles · qty) with **Open** / **Open all**. No migration — each SO's PO lives on its header; the per-line tag is stripped from line payloads. `ResolvedLine.customer_po` stamped in `LineMatrixBody.resolve()`; split + child-create in `InternalSalesOrders.commitSave`. Typecheck ratchet 0 new. Files: `src/tanda/LineMatrixBody.tsx`, `src/tanda/InternalSalesOrders.tsx`. User-guide ch27.
> - **PO grid modelled on the SO grid (cost/margin strip + Avg PO Price + row explode)** — the native Purchase Orders list (`InternalPurchaseOrders.tsx` + list endpoint `enrichPricing`) is reworked to read like the Sales Orders grid. **Avg cost** now means the item's **STANDARD / catalog cost** (`ip_item_avg_cost`, keyed by SKU exact→loose — the same source the Inventory Snapshot + SO grid use), and a **new Avg PO Price** column carries the qty-weighted unit price on the PO's *own* lines (what it previously showed as "Avg cost"); read together they surface **PO variance** (buying above/below standard). **Margin %** and a new **Margin $** column are now `Sell − Avg PO Price` (priced against the real buy, mirroring the SO Sell−Cost convention; both green/red). Added a **Cancel date** column (SO parity). The **Totals** footer + **Export** gain the Avg PO Price and Margin $ figures. Each row gets a leading **▸ carrot expander** → an inline **per-style color×size matrix** of that PO's lines with an **EXPLODE PPK** toggle (packs ↔ units + per-each cost; shared pref with the PO/Item Matrix tab); clicking elsewhere on the row still opens the PO modal. Server margin fields (`avg_po_price_cents`, `margin_cents`, `margin_pct`) are additive. Typecheck ratchet 0 new + vite build green. Files: `api/_handlers/internal/purchase-orders/index.js`, `src/tanda/InternalPurchaseOrders.tsx`. User-guide ch28.
>
> **Recent cross-cutting landings (2026-06-30)** — not tied to a single phase row:
> - **Audit Trail "Invalid or expired token" fix.** The read-only audit endpoints (`/api/internal/audit/row-history` — powering the **Audit Trail** panel in the AR/AP invoice, JE, customer/vendor/etc. detail modals — and `/api/internal/audit/log`) were the only internal-staff *read* endpoints gating on the **per-user** `authenticateCaller` (app-JWT or live Supabase session). When a user had no/expired per-user JWT — e.g. they entered Tangerine via the **#1476 PLM-session fallback** (which mints no per-user token), or past the 12h token expiry — the browser's fetch interceptor falls back to sending the **static deploy token** as the Bearer, which `authenticateCaller` rejects → **401 "Invalid or expired token"**, while every other panel worked (they use the deploy-token gate). Switched both endpoints to `authenticateInternalCaller` (the standard `INTERNAL_API_TOKEN` "is this our frontend" gate that accepts the static token via Bearer **or** `X-Internal-Token`, and fail-opens when unset) — correct for a SELECT-only surface whose rows already carry the actor. No per-user `authId` was used post-gate. Tests updated (row-history + log: 60 passing). Files: `api/_handlers/internal/audit/{row-history,log}.js`. User-guide ch06.
> - **SO batch items 22–25.** (**22**) The SO entry's two header labels above the Customer-PO row — **"Customer PO #"** and **"Or auto-fill from the customer's PO"** — were removed (the `Field` helper now omits an empty label); the PO input, its required warning + Generate-placeholder, and the **Upload customer PO** button are unchanged. (**23**) The **Fulfillment source** picker widened from 280→**420px** so the full *"Production — make it (notifies Production Mgr)"* option no longer truncates. (**24**) The inline **+ Add prepack matrix** popup on the SO/PO line matrix now renders the **identical** form used by the Prepack Matrices Master — the master's `MatrixFormModal` is exported and reused (with new optional `initialPpk`/`initialPackToken` prefill props), and the old simpler `QuickAddPrepackMatrix` is deleted, so the size-scale picker, inner-pack/units-per-inner-pack-or-carton entry, active flag, notes and pack-token validation are all available from the order. (**25**) The SO grid gains a **Total Margin $** column right after **Margin $** — since the server's `margin_cents` is the *per-unit* margin (`avg_sell − avg_cost`), Total Margin $ = `margin_cents × total_qty` (the order's full margin); added to the visible grid, the column-visibility toggle, and the Excel export. Files: `src/tanda/InternalSalesOrders.tsx`, `src/tanda/InternalPrepackMatrix.tsx`, `src/tanda/LineMatrixBody.tsx` (− `components/QuickAddPrepackMatrix.tsx`). Typecheck ratchet 0 new + vite build green. User-guide ch27.
> - **Phantom on-hand recurrence fix (Inventory Matrix on-hand integrity)** — the Matrix/Snapshot On Hand is `Σ inventory_layers.remaining_qty`, but for Xoro-sourced styles those layers were a one-time 2026-05-27 `opening_balance` seed that **Xoro sales never deplete** (`xoro-sales-sync` only writes `ip_sales_history`), so already-**sold** goods kept showing as on-hand — phantom overstatement (one-time data correction: 64 styles, 130,429 → 4,351 units). To stop it recurring, a new **nightly on-hand sync** (`api/_lib/inventory/onhand-sync.js`, hooked into `/api/planning/sync-on-hand`, env-gated by `ONHAND_LAYER_SYNC=report|apply`) rebuilds each Xoro-sourced style's on-hand layers from the **authoritative ATS feed** (`ip_inventory_snapshot` source='manual', latest date) into a dedicated `source_kind='xoro_onhand_sync'` (migration `20260929000000`). **Invariants:** acts only on SKUs in the latest feed (sold-through styles absent from the feed are left at their corrected value); manages a style **only** while every layer it has is Xoro-seeded — the moment a native event touches it (AP/PO receipt, adjustment, manufacture, transfer) or the by-size cutover (`xoro_rest_size`) owns it, the sync skips it, so it never double-counts native FIFO. Controlled rollout via `scripts/rebuild-onhand-sync.mjs --apply [--style …]` (dry-run default). Dry-run scope: 771 manageable / 299 skipped of 1070 feed styles. Memory `project_phantom_opening_balance_onhand`. User-guide ch11.
> - **SO "Confirmation" button (item 7 — consolidated + buyer email default)** — the Sales Order's bottom action bar now has a single **Confirmation ▾** button that replaces the old separate *View* + *✉ Email confirmation* buttons and opens a small menu: **Download to Excel** (new `downloadOrderExcel()` in `orderDocument.ts` — an `.xlsx` of the same header + per-style color×size matrix + totals the printable doc renders, xlsx dynamically imported), **Save as PDF / Print** (the existing branded printable doc, now opened straight into the print dialog via a new `autoPrint` option), and **Email confirmation to buyer…** (saved orders only). The email path now **defaults the recipient to the buyer on the order** (`buyer_id`) and merges the customer's **buyers** — which carry emails, from `/api/internal/customer-buyers` — with any legacy contacts (deduped by email) so that if the order has no buyer the operator picks one from the customer master; doc-attach selection is unchanged. The server-side branded HTML + Resend send endpoint (`POST /api/internal/sales-orders/:id/email-confirmation`) and `EmailSOConfirmationModal` already existed (#1463) — this surfaces them behind one button and adds the Excel/PDF/buyer-default. Files: `src/tanda/orderDocument.ts`, `src/tanda/InternalSalesOrders.tsx`, `src/tanda/components/EmailSOConfirmationModal.tsx`. Typecheck ratchet 0 new + vite build green. User-guide ch27.
> - **ATS main-grid reciprocal cascade (#9, batch-G residual closed)** — the ATS grid toolbar's **Category / Sub Cat / Style / Gender** dropdowns now narrow **reciprocally**: each dropdown's options reflect the rows that pass *every other* active filter (search + the other dropdowns + store + status), so picking a Style only shows the Categories/Genders that style has, picking a Gender prunes the Styles, etc., and clearing one re-widens the others. Stale selections that fall out of scope are auto-dropped (prune effects). Previously only Sub Cat (←Category) and Style (←Cat+SubCat) cascaded one-directionally and Gender was a fixed `M/B/C/Wms/G` list. Built on the same `filterRows` predicate the grid uses, so options never diverge from results. **Scope guard:** these cascaded lists feed the **toolbar only** — the full `categories/subCategories/styles` lists passed to **Sales Comps** are left untouched so an operator can still broaden a report past the grid's current filter. Implemented in `src/ats/renderPanel.tsx` (grid* lists + prune effects) + `src/ats/panels/Toolbar.tsx` (new optional `genderOptions` prop); `src/ATS.tsx` and `filter.ts` untouched. Typecheck ratchet 0 new + vite build + 357 ATS tests green. Closes the lone residual from batch G. App docs `docs/apps/ats-overview.md`.
> - **Inventory Planning + Vendor portal UI sweeps (sweeps #5/#21/#23/#9, batch G)** — app-wide consistency pass over `src/inventory-planning/*` and `src/vendor/*`. (#5) **Sortable columns** — every planning data table that lacked sort now wires the shared `useSort` + `SortableTh` tri-state (▲ asc → ▼ desc → off, persisted): Future Demand Requests, Forecast Accuracy, Override Effectiveness, Scenario Comparison/Assumptions/Manager (+ exports history), Execution Batch Manager + Batch Detail (actions), Supply Exceptions, Build Reconcile preview, Data Quality, Integration Health (both tables), Roles assignments, Runs Admin, AI Demand, Anomaly Queue (Audit Explorer + Job Runs already had it). The vendor portal's CSS-grid list views (Purchase Orders, Invoices, Shipments, RFQs, Contracts, Disputes) gain local click-to-sort headers with the same tri-state. Computed/JSX/action/inline-editor columns stay inert by design. (#21) **Select-all-on-focus** — search/filter text boxes across both modules now `select()` their contents on focus (planning grids + Audit Explorer; vendor Shipments/PO-Messages/Phases search bars). (#23) **Export totals** — the Reports Excel download appends a **TOTAL** row summing number/currency columns (percent/date/text left blank). (#9) **Cascading filters (bounded)** — the Ecom Planning grid's **Channel** and **Category** dropdowns now narrow reciprocally to the rows passing every other active filter + search, dropping a now-invalid pick (Wholesale/Reconciliation/Future-Demand already cascaded; single-filter lists skipped). Presentational-only — planning compute/export tests stay green. User-guide ch07 (planning) + ch10 (vendor portal).
> - **ATS + Costing UI sweeps (sweep G — #5/#21/#23)** — applied the app-wide polish sweeps to the **ATS** and **Costing** modules. **#21 select-on-focus:** every search/filter text box now selects its existing text on focus — ATS toolbar (SKU search + the Category/Sub-Cat/Style/Brand/Cust-Vend dropdown search fields), the ATS **Sales Comps** + **Export Options** modals, and the Costing **RFQ list** + **Messages inbox** search boxes. **#5 sortable columns:** the **Sales Comps** comparison tables (per-customer/category/style/SKU) gained **click-to-sort headers** (functional ▲▼) across TY/LY Qty·Rev·Mrgn%, Δ Rev, Δ Mrgn pp, and the dimension label; default order stays TY-rev-desc and the TOTAL row(s) stay pinned. (Costing Projects + RFQ lists already sort via the shared `useSort`/`SortableTh`.) **#23 export totals:** the Costing **project grid** Excel/PDF export now appends a **TOTAL row** (Σ qty, Σ total cost, Σ total sales, weighted margin %) via the pre-existing `computeExportTotals`, matching the on-screen grid footer. **Out of scope / skipped:** **#9 cascading filters** for the main ATS grid — the Category/Sub-Cat/Style option derivation lives in `src/ATS.tsx` (outside the `src/ats/*` ownership boundary for this sweep); Costing list views are single-filter (search + one status dropdown) so the bounded #9 rule excludes them. Files: `src/ats/panels/{Toolbar,SalesCompsModal,ExportOptionsModal}.tsx`, `src/costing/views/{RfqListView,RfqMessagesInbox,ProjectEditView}.tsx`. See `project_sweep_ats_cost_g`; app docs `docs/apps/ats-overview.md` + user-guide ch03.
> - **Select-all-on-focus for shared search inputs (#21, shared layer)** — the shared, reused search/filter primitives now **select all their text on focus**, so tabbing or clicking into a populated search box highlights the existing query and the next keystroke replaces it (no manual select-all/backspace). Touched the **shared layer only**: `DynamicSearchInput` (the universal type-as-you-go search box, `onFocus` added) and the `SearchableSelect` typeahead (composed into its existing focus-opens-panel handler — when re-focusing an already-open popover the query text is selected; opening still clears the filter as before). Per-grid inline inputs are swept by the parallel module agents. Skipped non-search shared inputs (StagedDocsPicker/DocumentAttachmentList data fields, AppDatePicker, EditableSizeMatrix cells, the warn prompt) where caret placement matters. **ExportButton** also gained an **optional backward-compatible `totalsRow` prop** (`Partial<T>`, default off) that appends a final totals row to the export — bold in xlsx, plain final row in csv, bold border-topped row in pdf — for module agents that want to mirror an on-screen Totals strip into the download. No default behaviour changed. Files: `src/tanda/components/DynamicSearchInput.tsx`, `src/tanda/components/SearchableSelect.tsx`, `src/tanda/exports/ExportButton.tsx` + `useTableExport.ts`.
> - **OPS / Inventory grid sweeps (wave G — #5 / #21 / #23 / #9)** — applied four cross-cutting UI sweeps to 18 Tangerine OPS/INVENTORY `Internal*` grid panels (`InternalReceiving`, `ShipmentsView`, `InternalThreePL`, `InternalAllocations`, `InternalCycleCounts`, `InternalThreePLRecon`, `InternalDropShip`, `InternalSalesReturns`, `InternalShopifyRefunds`, `InternalReconciliationDashboard`, `InternalEDI`, `InternalEdiCustomers`, `InternalMarketplaceStatus`, `InternalMarketplaceInquiries`, `InternalSustainability`, `InternalSizeScales`, `InternalWorkspaces`, `InternalThreeWayMatch`). **#5 Sortable columns** — every list `<table>` header now offers tri-state click-to-sort (▲▼) via the shared `useSort` + `SortableTh`, persisted per panel (`tangerine:<panel>:sort`), with accessors for derived/computed columns; editable entry-line grids, expanded sub-detail tables, and CSS-grid-of-`<div>` lists are intentionally left inert (SortableTh emits a `<th>`). **#21 Select-all-on-focus** — inline free-text search/filter inputs select their contents on focus so a re-search overtypes cleanly (shared `SearchableSelect`/`MultiSelectDropdown` inputs are handled centrally and untouched). **#23 Export totals** — Excel exports that carry numeric/currency columns now append a **TOTAL** footer row (Σ of the numeric columns; text columns blanked; suppressed on empty datasets). **#9 Cascading filters (bounded)** — applied to `ShipmentsView` (vendor + status dropdowns + search now narrow each other reciprocally, keeping the active selection clearable); all other panels had a single category filter or no search box, so the bounded precondition wasn't met and they were left as-is. `InternalCycleCounts` already had sort and `InternalMarketplaceInquiries` is a card list with text-only export — both required no change. Typecheck ratchet (0 new; 2 baselined errors incidentally resolved) + vite build green. User-guide ch11 (suite grids).
> - **Merge-PPK drill-through (Snapshot)** — each merged **`BASE/PPK`** line in the Inventory Snapshot now carries a **▸ expander** next to its style code; click it (or Enter/Space) to reveal the **two components that summed into the merged line** — a **Base eaches** row (the base style) and a **PPK pack** row (the PPK sibling, exploded to per-unit eaches via the #1478 basis: a pack of 24 @ $120 = $5/each). The component rows are indented + purple-tinted, labelled which side they are, show the underlying style code, and **reconcile exactly** to the merged totals (the merge is a plain per-unit sum of these same rows). Each component still drills to **its own** style (On Hand → matrix, On SO/PO/ATS/Allocations → those windows, Sold/Purchased → the detail modals). The drill keeps the existing component `SnapshotRow`s on the merged row (`MergedRow._components`) and reuses the same row-expand affordance — no API change. **Scope:** merge-PPK only renders in the Snapshot (SO/PO/style-matrices use the separate per-line **prepack pack-token** entry, not a merged line, so there's nothing to drill there); when **Collapse onto Style/Item Category** is also active the merged lines fold into multi-style groups with no single drill target, so the expander is not offered there. Single-file change in `src/tanda/InternalInventoryMatrix.tsx`; user-guide ch11 + ch28.
> - **Purchase Orders grid batch (PO batch G)** — the native PO list (`InternalPurchaseOrders.tsx`) gains: a **Margin %** column = `(Sell − Avg cost) ÷ Sell` (one decimal, green/red), reusing the **same cost basis** (PO qty-weighted unit cost from `avg_cost_cents`) and **same sell reference** (`sell_cents`) the existing Avg-cost / Sell-price columns use — computed client-side, no API change, mirrors the Sales-Orders margin convention; **per-column click-to-sort** (tri-state ▲▼ via the shared `useSort` + `SortableTh`, persisted); a pinned **Totals** footer (Σ Total + total-weighted Avg cost / Sell / Margin %) with a **Filtered rows / All rows** scope toggle (filtered = client date-window subset, all = whole loaded dataset); the **Excel export** now appends that Totals row (honouring the scope). **Open-PO status colour** — the modal title + PO-number/status chip now carry the grid's `STATUS_COLORS` (draft grey · issued blue · in-transit amber · received green · cancelled red). **Lot-bubble font** — the shared `EditableSizeMatrix` Lot input (PO/SO) uses a compact font + a touch more width so a full lot/customer-PO number fits; the Split-by-customer-PO chips shrink + wrap. User-guide ch28.
> - **Financial-grids UI sweep batch (sweep batch G — sortable / select-on-focus / export-totals / cascading)** — the Tangerine **financial** list panels (`InternalARInvoices`, `InternalARReceipts`, `InternalAPInvoices`, `InternalJournalEntry`, `InternalTrialBalance`, `InternalPayments`, `InternalFixedAssets`, `InternalScf`, `InternalVirtualCards`, `InternalDiscountOffers`, `InternalVendorMaster`) get four cross-cutting refinements. **(#5) Per-column click-to-sort** (tri-state ▲▼ via the shared `useSort` + `SortableTh`, persisted per panel) on every flat data table — added to AR Invoices, AR Receipts, AP Invoices, Journal Entries, Fixed Assets; for the four **div-grid "tables"** (Payments, SCF requests, Virtual cards, Discount offers) a tiny local clickable `SortHeader` cell mirrors the same ▲▼ affordance off the same `useSort` hook (SortableTh needs a `<th>`). **(#21) Select-all-on-focus** on inline filter text inputs defined in-file — applied to the Journal Entries search box (the other panels search via the shared `DynamicSearchInput`/`SearchableSelect`, handled centrally). **(#23) Export totals** — Trial Balance's Excel export now appends a **GRAND TOTAL** row (Debits / Credits / Net) mirroring the on-screen tfoot, and AR Receipts appends a **TOTAL (active)** row (non-void Amount + Σ Applied / Unapplied) mirroring the on-screen "Active total". **(#9) Cascading filters** — **none applied**: the AR/AP/JE filter dropdowns are server-side and back fixed status/source/basis enums (not category-from-data), and Payments/SCF/Cards/Discounts each expose a single category dropdown, so the bounded cascading rule didn't fit. Vendor Master was already sortable + searched via the shared input, so it needed no change. Sort skipped on the **grouped, subtotalled** Trial Balance table (sorting would break the account-type grouping). User-guide ch04 (AR/AP) + ch05 (JE/TB) + ch07 (treasury).
> - **Open Tangerine from the launcher without a second login** — selecting **Tangerine ERP** from the PLM launcher (or any 🧩 Apps menu) no longer prompts the operator for a **redundant second sign-in**. Tangerine's identity is normally a Microsoft-365 token, but a launcher user authenticates once with **username/password** (written to `sessionStorage.plm_user`, cloned into the app tab on open) and previously hit Tangerine's own **"Sign in with Microsoft"** wall on top of that. Tangerine's mount auth gate now, when there is **no usable MS token**, **adopts the PLM session** (`readPlmSessionIdentity` → email/name; sets `authState=signed_in`) instead of rendering `LoginScreen`. The MS-only extras (Graph profile photo + the per-user **JWT provisioning** bridge that needs `ms_access_token`) simply stay absent until the user signs in with Microsoft — both are already **best-effort/non-fatal**, and the internal API stays gated by its own server token (`internalApiAuth` is fail-open on the static deploy token), so nothing is blocked. **Kept gated:** the standalone front door (`/login` and a cold, no-session `/tangerine`) still shows the Microsoft sign-in; the `/tangerine` **route guard** in `main.tsx` still blocks anyone explicitly set to `tangerine.access=false`. See `project_app_no_relogin_g` + `project_two_permission_systems`. Single-file change in `src/Tangerine.tsx`; user-guide ch01 (suite + Tangerine) + ch11.
>
> **Recent cross-cutting landings (2026-06-28)** — not tied to a single phase row:
> - **Inventory Snapshot drill popups — sorts, store/collapse filters, bill-500 fix, modal navOffset** — (#5/#7) every Snapshot grid + Sold/Purchased popup header now sorts (`useColumnSort`, ▲/▼ toggle); the **Sold** popup gains a **Store** filter; (#18) a **Collapse: Invoice** toggle (Sold) and (#17) a **Collapse: Bill** toggle (Purchased) roll the detail rows up onto invoice/bill number. (#23) with **Totals** on, the Snapshot Excel export appends the Totals rows (Qty / $ Cost / $ Wholesale / Avg Cost / Avg Sale). (#16) the bill drill threw a **500** — `ap-invoices/[id].js` ordered `invoice_line_items` by a non-existent `line_number` column (the real column is `line_index`); fixed the order + the PATCH insert + the `DocDetailModal` AP line shape (`line_index`/`line_total` vs AR `line_number`/`line_total_cents`). (#15) AR invoice popup + Edit already worked. (#24) the drill modals now clear the NavDrawer via a new `--tng-nav-offset` CSS var Tangerine publishes (`left`/`width` derive from it) so a centred panel never slides under the menu on a wrapped view. User-guide ch11.
> - **Inventory Snapshot filters — Warehouse filter now works, cascading dropdowns, Collapse over the full set** — (#8) the Snapshot **Warehouse** dropdown previously had no effect (the snapshot POST never sent it); `inventory-snapshot.js` now accepts a `warehouse` param and narrows the **On Hand** column to that warehouse's `inventory_layers` (resolved via `location_id → inventory_locations.name`, legacy `wh=` notes fallback) plus the ATS on-hand source by `warehouse_code`. (#9) the **Gender / Group / Category / Sub-Category** dropdowns now **cascade reciprocally** — each offers only the values present among styles matching the *other* active filters + search (`matchesExcept`). (#13) **Collapse** now aggregates the **full filtered set** (all matching styles, capped 4000), exactly like Export — `snapStyleIds` switches from the page to all-filtered when any Collapse column is checked, and the pager is replaced by an "all N styles" note. User-guide ch11.
> - **Inventory Snapshot Totals — per-unit explode fix + Avg Cost/Avg Sale + Wholesale rename** — the Totals strip now adds **Avg Cost** (= $ Cost ÷ qty) and **Avg Sale** (= $ Wholesale ÷ qty) lines per column, and the old **$ Retail** line is relabelled **$ Wholesale** (the underlying `sale_price_cents` is the SO `unit_price`, i.e. the wholesale selling price — not a retail list). Fixed the "millions" totals bug on PPK styles: `inventory-snapshot.js` exploded every qty column ×units-per-pack but left a PPK SKU's avg cost at **pack** grain (e.g. $136.80 for a 24-pack), so $ Cost = eaches × pack-cost read 24× inflated; the handler now divides the pack cost by the same multiplier to a per-each cost ($5.70). The frontend **auto-forces per-unit explode** whenever **Totals** is on and the visible page contains any PPK style (`effectiveExplodePpk`). User-guide ch11.
> - **Style renumber + aliases (#1453)** — operators can now **renumber a style** in Style Master (e.g. drop a legacy Xoro inseam infix `RYB147730` → `RYB1477PPK`) without losing history. New `style_master.aliases text[]` (mig `20260922000000`, GIN idx, applied prod) mirrors vendors/customers.aliases. The Style Number field is **editable on edit**; on a change the PATCH **captures the old code as an alias**, **cascades the new code to `ip_item_master` (keeps `sku_code` stable)**, and **re-keys `prepack_matrices`**. All transactional history is UUID-keyed (ip_item_master.style_id, *_lines.inventory_item_id, inventory_layers.item_id, ip_sales_history_wholesale.sku_id) so it stays attached automatically; aliases cover string-grain resolvers — the **Xoro importer `loadStyles`** now indexes aliases (legacy codes still import to the renamed style) and the **prepack matcher** already bridges. ATS needs no change (resolves by stable `sku_code`). Style Master gains an **Aliases** chips field (auto-filled on renumber, hand-editable). `normalizeAliases`/`appendAlias` pure helpers +8 unit tests. User-guide ch02 + CURRENT-SCHEMA.
> - **Prepack pack entry on SO/PO (#1451)** — adding a **PPK (prepack) style** to a Sales Order or Purchase Order line matrix now renders a **pack-entry grid** (one column = the pack token, e.g. `PPK24`) instead of the garment-size grid: the operator types the **number of packs** per color (unit money **per pack**), with a **▾ size breakdown (explode)** toggle showing `packs × the per-size composition` from the **Prepack Matrix master**. The order line **stores packs** (native pack grain); the size breakdown is derived (the same explode the Inventory Matrix uses). Fixes the prior dead-end where a PPK style either fell into "no size scale" (non-matrix) or rendered a full S–XL grid with no pack input. Pack entry takes precedence for **any** PPK style regardless of an errant size scale; a PPK style with no matrix still accepts packs but shows a "define a matrix" hint. Additive `prepack` block on `GET /api/internal/style-matrix` (`computePrepackBlock` in `styleMatrix.js`); pure `explodePacks`/`packTotal` in `src/shared/prepack` (+unit tests); `LineMatrixBody` (shared SO/PO/AR) branches on it. User-guide ch27/28.
>
> **Recent cross-cutting landings (2026-06-26)** — not tied to a single phase row:
> - **Notifications now deep-link to their task across all apps (#22)** — clicking ANY notification opens the actual record it refers to instead of being inert. Two shared resolvers route every type through one place: `src/components/notifications/notificationLink.ts` for the `notifications`-table surface (**vendor bell** + **cross-app NotificationsPage**: PO WIP / Design Calendar / GS1 / Tech Packs) maps `event_type` + `metadata` (po_id/po_number/so_number/rfq_id/invoice_id/dispute_id/contract_id/…) → vendor route or `/tangerine?m=…&q=…`; `src/tanda/notificationTarget.ts` for the Tangerine **Notification Center** (`notification_events`) maps `context_table` (sales_orders, purchase_orders, ar_invoices, invoices→ap, customers, vendors, gl_periods, inventory_adjustments, inventory_cycle_counts, crm_tasks, rfqs, entities) + `payload` (so_number/invoice_number) → module + drill params. Records are addressed by **human reference** (order/invoice number) — no raw UUIDs. Already-specific producer links are kept; weak links (app home, bare list) are **upgraded** from metadata; an exact record that can't be addressed **falls back to the owning module/list**; system/run events (xoro mirror runs) just mark-read. Producer link fixes: SO production-order notification now `&q=<so_number>`, vendor-quote internal notification now `/tangerine?m=rfqs` (was `/`). +14 unit tests. User-guide ch08.
> - **AI postal-code fill + postal key fix (#7)** — the address editor (`AddressFields`, used by customer billing/shipping + locations + vendors) wrote/read `postal_code`, but every Xoro-synced address stores the postal under **`postal`** (1,385 rows; 0 `postal_code`) — so synced postals never displayed. Aligned the app on **`postal`** (read `postal ?? postal_code` for legacy; write `postal`) across the editor, location summaries, and SO ship-to display. Added a **🤖** button on the postal field that calls a new `POST /api/internal/addresses/postal-suggest` (Claude Haiku) to fill the postal (US ZIP/ZIP+4 when determinable, else 5-digit) from the rest of the address; the endpoint also has a `{bulk:true}` mode that fills every customer billing/shipping + location address missing a postal (city+state present, fill-only, re-reads to avoid clobbering the concurrent sync).
> - **Customer-contact notes + reminders (#12)** — each AP/Trans/CB contact gets timestamped **notes** (author + date/time) and an optional **reminder**. New `customer_contact_notes` table (mig `20260904000000`, applied prod) keyed by a stable `id` now stamped on each `customers.contacts[]` entry (sanitizeContacts preserves it). `GET/POST/DELETE /api/internal/customer-contact-notes`; a 📝 Notes panel under each contact (component `CustomerContactNotes`) lists notes + adds one with an optional `datetime-local` reminder (author = current user via `getCachedAuthUserId/Name`). An hourly cron `customer-contact-reminders` finds due reminders and creates an internal `notification_event` + `notification_dispatch` for the user who set it; clicking that bell notification (`InternalNotificationCenter` now deep-links `context_table='customers'`) opens `?m=customer_master&open=<id>&contact=<id>&note=<id>` → the customer's AP/Trans/CB tab with that contact's notes open + the note highlighted. Also #11: removed the descriptive blurb from the AP/Trans/CBs tab.
> - **Customer Master — AP/Trans/CB contacts tab** — new **AP/Trans/CBs** tab holding up to **8** department contacts, each with **Department** (Accounts Payable / Transportation / Chargeback), name, email, phone (stored in the existing `customers.contacts` jsonb; backend already accepts it). The **Contact information** block (name/title/email/phone/website/WeChat) was **removed from the Details tab** (columns kept on the record, just not edited there). Row actions are now **three separate fully-framed buttons** (Scorecard / Edit / Delete) inline instead of a joined bar.
> - **Per-style returns routing (credit memos)** — customer credit memos (M23 RMA) now route each return line's revenue reversal to the style's `returns_account_id` (brand Sales Returns: 4236/4234/4235/4201) → customer `default_returns_account_id` → entity 4100, mirroring the per-style revenue/COGS routing. `buildCreditMemoLines` takes a per-item returns map; the credit-memo handler resolves it from each line's style. +unit test.
> - **Per-style GL routing** — each style now posts its sales to the GL accounts for its brand bucket (ROF Brands / Boys / PT / Private Label), matching how Xoro routes by item. New `style_master.revenue_account_id` / `cogs_account_id` / `returns_account_id` + `ar_invoice_lines.cogs_account_id` (mig `20260903000000`, applied prod). **Backfilled 1,961 of 2,100 styles** from the Xoro item GL export (`BasePartNumber` → bucket → Tangerine code; saved `scripts/backfills/xoro_gl_account_map.json` + `style_gl_from_xoro.sql`). At AR-invoice creation each line's **revenue + COGS account resolves style → customer default → entity default** ([create-invoice.js](api/_handlers/internal/sales-orders/create-invoice.js)); the `arInvoiceSent` rule now books **COGS per line** (`ln.cogs_account_id ?? invoice default`), the same way revenue was already per-line — so sales + COGS land in each brand's accounts. Unmapped styles (~139) fall back to customer/entity defaults. +unit test. Returns account is stored per style (credit-memo wiring is a follow-up). Marketplace order JE posters are separate and unchanged.
> - **Closeout-order commission (Customer Master batch 2)** — sales reps earn a different rate on closeout orders. New `customers.closeout_commission_pct` (Reps tab field) + `sales_orders.is_closeout` (a **Closeout order** checkbox on the SO header), mig `20260902000000` (applied prod). The **Customer Scorecard** commission math now splits This-Year net sales into closeout vs normal (by each invoice's SO `is_closeout`) and applies the closeout rate to the closeout portion, the rep rate to the rest; reports `closeout_sales_cents` + `closeout_commission_pct` and a blended effective `commission_pct`. Dilution apportioned proportionally.
> - **Customer Master — display polish (batch 1 of N)** — every default-* picker (brand, channel, payment terms, GL routing accounts, price list, country) now shows the **name only** (the code stays searchable) per operator request; sales-rep and factor pickers were already name-only. The **Credit limit** field is now a **comma-formatted whole-dollar** input (up to 12 digits, e.g. `1,250,000`, no decimals) instead of an 8-char decimal box. Part of a larger Customer Master batch (closeout commission, default-brand backfill, GL/postal backfills) landing across follow-up PRs.
> - **Customer Master — GL field de-dup** — removed the redundant **Default AR / Default revenue** pickers from the **Details** tab (the legacy `default_gl_ar_account_id` / `default_gl_revenue_account_id` columns, which **no posting engine ever read**). The **GL Accounts** tab (`default_ar_/revenue_/returns_/cogs_account_id` — the columns the SO line-revenue routing + AR invoice/receipt posting actually consume) is now the single source of truth. Handlers no longer write/return the legacy pair; columns kept in the DB (reversible, no migration). Prod had identical values across both families and zero legacy-only rows, so no data migration needed.
>
> **Recent cross-cutting landings (2026-06-22)** — not tied to a single phase row:
> - **SO customer-PO upload — AI customer matching + deterministic apply** — the confirm-choices customer pick now defaults to an **AI semantic match** (`POST /api/internal/sales-orders/match-customer`, Claude Haiku): given the parsed PO customer name it picks the best account in the master by meaning (e.g. "Ross Stores, Inc." → "Ross Procurement"), not by incidental word overlap, with a one-line reason; the operator still confirms/overrides. Also hardened the confirm→apply path: the resolved style list + fetched matrices computed for the questions are reused verbatim by `applyParsed` (no re-resolve against state, no cache clear, `setCustomerId` not `pickCustomer`), fixing a case where the prefilled qtys/price could come back empty after confirming. User-guide ch27.
>
> **Recent cross-cutting landings (2026-06-18)** — not tied to a single phase row:
> - **SO customer-PO upload — confirm-choices + duplicate guard** — the 🤖 Upload customer PO flow now (1) **asks instead of guessing**: when the parsed customer didn't match exactly, or a PO colour didn't map cleanly onto a style's actual colour row, a **Confirm choices** step lets the operator pick (alongside the existing base/PPK pick) *before* the order is filled — resolved picks no longer show up as "verify"/"pick manually" lines in the after-the-fact banner. (2) **Duplicate guard**: before prefilling, the PO # is checked against existing non-cancelled SOs (`GET /api/internal/sales-orders?customer_po=` exact, case-insensitive); a hit blocks the prefill with an "already exists" warning + **Open existing SO** / **Cancel** — no duplicate SO is created. Helper gains `customerCandidates` / `matchCustomerExact` / `computeColorQuestions` / `colorPickKey` + a `colorPicks` override on `buildSeedFromResolved` (+unit tests). User-guide ch27.
> - **Color Master NRF code (AI-matched)** — `color_master` gains `nrf_code` + `nrf_name` (NRF standard 3-digit colour-family code, mig `20260895000000`, applied prod). New `POST /api/internal/colors/nrf-suggest` (Claude Haiku): single mode `{name, hex?}` → suggested `{nrf_code, nrf_name, confidence}` (no write); bulk mode `{bulk:true}` matches+writes every colour missing a code in batches (UI loops to completion). Color Master gets an **NRF** grid column, a header **🎨 Auto-match NRF (AI)** bulk button, and a **🤖 Suggest** button on the add/edit modal's new NRF field (re-runnable whenever the name/swatch changes). User-guide ch02.
> - **3PL EDI goods-receipt advice (X12 944)** — new `POST /api/internal/edi/tpl/:provider_id/receipt-advice` accepts an X12 944 (lenient `parse944`) or a structured `{po_number, lines:[{sku, qty_received}]}` / `csv`, resolves the native PO + maps each SKU to its line, logs the raw advice to `edi_messages`, and creates a **DRAFT** `tanda_po_receipts` (native path). It does NOT auto-post — the draft lands in **Receiving** for the operator to confirm + post (which books FIFO + GR/IR via the #1365 flow), so an EDI receipt keeps human confirmation. +parse944 unit tests. User-guide ch28.
>
> **Recent cross-cutting landings (2026-06-15 →)** — not tied to a single phase row:
> - **AP per-bill GL posting — vendor default expense account (#1666, 2026-07-08)** — enhancement to #1662: when a bill's vendor has `vendors.default_gl_expense_account_id` set, the sweep (`runApPostingSweep`) routes the non-item/tax-plug line there instead of fallback 8007 Uncategorized Expense. Vendor defaults are batched once per sweep and validated against `gl_accounts` (same entity, `is_postable`, `status='active'`, not `is_control`) — any lookup/validation failure silently falls back to 8007, so posting never blocks. `composeApBillJe` stays pure: it just prefers `accounts.vendorExpense || accounts.fallbackExpense`. +2 unit tests. User-guide ch13.
> - **Financial report panels — fetch-race guard (#1663, 2026-07-08)** — new shared hook `src/tanda/hooks/useSeqGuard.ts` (the proven `loadSeqRef` sequence pattern from InternalSalesOrders): `begin()` claims a load ticket, `isCurrent(seq)` gates every setState after an await, so a slower earlier response can never clobber the newest filter/date-range state. Applied to the `load()` of all nine report panels: Income Statement, Balance Sheet (both fetches), Trial Balance, Cash Flow, Segment P&L, GL Detail (clearing the account also invalidates in-flight loads), Sales by Customer, Sales by Rep, CRM Pipeline Report. No pagination changes needed — all nine hit `/api/internal/*` endpoints that aggregate server-side (no direct PostgREST reads, so the 1000-row cap doesn't apply). User-guide ch38.
> - **CI hardening — production-build + lint gates (#1664, 2026-07-08)** — per the ops audit, `.github/workflows/test.yml` gains two jobs alongside `test`: **`build`** runs the real `vite build` (vitest+tsc never exercise rollup, so a bad import/missing asset could pass CI and only break the Vercel deploy), and **`lint`** runs `npm run lint` — a NEW minimal high-signal ESLint flat config (`eslint.config.js`, ESLint 10) over `src/**/*.{ts,tsx}`: core correctness rules only (`no-dupe-keys`, `no-debugger`, `use-isnan`, `valid-typeof`, `no-cond-assign`, …), zero errors required, warnings allowed (`no-unreachable` + `react-hooks/rules-of-hooks` demoted to warn — 40 pre-existing legacy hits; tighten later). react-hooks + @typescript-eslint plugins registered only so pre-existing inline disable comments resolve. Also fixed the one real `no-dupe-keys` hit (`src/shared/colorHex.ts` duplicate `sangria`, identical value — behavior-neutral). Vitest coverage floor skipped: no coverage provider installed / no baseline, not trivially achievable.
> - **AP per-bill GL posting (#1662, 2026-07-08)** — remediates the re-rate's confirmed silent defect: ZERO AP JEs had ever posted (the daily summary credited control 2000 with no vendor subledger → trigger-rejected nightly, swallowed). New pure composer `api/_lib/accounting/apBillPosting.js` (DR 1201 item-linked / DR 8007 non-item+tax plug / CR 2000 vendor-subledgered; credit memos flip; `journal_type='ap_invoice_historical'` rides the period bypass) + sweep runner `POST /api/internal/ap-backfill/run` {dry_run,limit} (internal-token gated, idempotent, heals the died-between-post-and-update case) + auto-sweep at the end of every `/api/ap/sync-bills` ingest. 6 unit tests. Also deleted 10 junk $0 `xoro_mirror` bill stubs (prod SQL, zero refs). Backfill posts the 142 unposted Xoro bills (~$7.15M into 2000). Payment-side JEs deferred (CSV has status only). User-guide ch13.
> - **Security enforcement sprint (#1661, 2026-07-08)** — actioned the re-rate's 3/10 security finding. (1) **Financial-table RLS**: dropped the blanket `anon_all_*` FOR ALL policies on 18 ledger/AR/AP/bank/commitment tables (mig `20260964000000`) — the browser anon key can no longer read/write the GL; service-role API handlers and the entity-scoped `auth_internal_*` + vendor `vendor_own_*` policies are untouched. (2) **CORS clamp**: `api/dispatch.js` wraps `res.setHeader` so every handler's `Access-Control-Allow-Origin: *` is rewritten to an app-origin allowlist (apps.ringoffire.com, prod/preview vercel.app, localhost dev) — one central change instead of ~700 handler edits; `resolveCorsOrigin` unit-tested. (3) **Verified identity on planning endpoints**: `ip-permissions.js` now takes the caller's email from the signed app-JWT (`verifyAppJwt`) when the bridge is configured, ignoring the spoofable `x-user-email` header (dev fallback only without the secret). (4) **`RBAC_MODE=enforce` set in prod** (was `log`; both internal users verified with full effective permission sets — 113/107 rows — and enforce fails open for legacy-token callers, so zero day-one breakage). ⚠️ gotcha: `echo | vercel env add` appends `\n` and `rbacMode()` doesn't trim — use `printf`. User-guide ch24.
> - **PO "received" is now real (receipt-driven), not a fake flip** — the full P13 receiving→GL→AP 3-way-match already existed for native POs (`procurement/receipts` posts FIFO layers + the GR/IR JE `DR Inventory / CR GR/IR 2050`; `vendor-invoice-drafts` clears GR/IR via `ap_invoice_grir_match`; bookkeeper queue finalizes) — but two wires were missing: posting a receipt never rolled the native PO, and the PO modal faked "received." Fixed: `receipts/:id/post` now bumps each `purchase_order_lines.qty_received` + sets the header `received`/`in_transit`; `purchase-orders/[id]` PATCH **rejects a manual `status:'received'`**; the PO modal's fake "📥 Mark received" is replaced by **📥 Receive** → opens Receiving for that PO (`?m=receiving&po=`, which now auto-opens a new receipt). GR/IR account 2050 already existed (no new account). ⚠️ Remaining: EDI 3PL inbound → auto-create/post a receipt (manual receiving works today). User-guide ch28.
> - **PO ✎ Edit (revise an issued PO) + vendor notification** — re-opening a saved PO now shows a **✎ Edit** button in the footer that unlocks the full header + lines for revision (ends the cancel-and-recreate workaround). **💾 Save revision** PATCHes `{revise:true,…}` (server lifts the draft-only line lock for that save) and fires a **"PO revised" notification** to the vendor's portal users (bell + email via `notifyVendor`/`vendor_users`; no-op if none). Audited via the existing T11 trigger. ✅ Follow-up landed (#1660): revisions now apply lines as a **diff that preserves line ids** — the old delete-all+reinsert 500'd on any received PO (`tanda_po_receipt_lines` FK RESTRICT) and CASCADE-wiped `po_commitments` + `po_shipment_lines` (D3 commitments + the in-transit overlay silently vanished on every ✎ Edit); `po_commitments` are **re-derived** after a line-changing revision (kept lines refresh committed amount + status vs consumption, new lines seed `open`, removed lines drop theirs, receipt consumption stranded on a removed line re-applied oldest-first), and removing an already-received line is rejected with a 409. (The receiving → GL → AP 3-way-match rework itself shipped earlier — see P13.) User-guide ch28.
> - **Customer Scorecard — in-place detail popups** — removed the **"Drill to:"** bar (top) and per-tab **"Open in … ↗"** links (bottom). Each row in the **Invoices / Sales Orders / Journal Entries** tabs is now clickable → a **popup over the scorecard** (header + line items; JE accounts resolved to `code — name`, no UUIDs) with **✎ Edit (new tab)** → the matching module and **✕ / backdrop / Esc** to return to the scorecard. New shared `DocDetailModal` in `CustomerScorecard.tsx` over the existing `ar-invoices|sales-orders|journal-entries/:id` GETs. User-guide ch30.
> - **AP bill PO number → Receipt Date on the Purchased drill** — the AP-bill sync now also captures each bill line's **PO number** (`billItemLineArr[].PoNumber`) into `invoice_line_items.po_number` (migration `20260894000000`, idempotent; `rest_ap_sync.py` adds a `PO Number` CSV column; `ap-bill-sync.js` parses + writes it). The Purchased drill **bridges that PO → the goods-receipt feed** (`ip_receipts_history.po_number → received_date`) to show a **Receipt Date** alongside the Bill Date on bill rows (best-effort; blank when no receipt matches). Re-run the AP sync to back-fill PO numbers. User-guide ch28.
> - **AP bill sync links bill lines to SKUs (fills the Purchased drill with history)** — the Xoro AP-bill sync's `buildLineRows` previously left `inventory_item_id` NULL, so synced vendor bills never showed in the Inventory Snapshot Purchased drill. Now `api/_lib/ap-bill-sync.js` reconciles each bill's Xoro **Item Number → ip_item_master** (`makeItemResolver`: exact sku_code loose-match → `style·color·size` tuple → representative colour SKU for colour-grain bills) and `api/_handlers/ap/sync-bills.js` builds that resolver from the referenced styles (paginated OR-ilike) and links every line; it also writes `unit_cost_cents` next to the legacy `unit_price`. The purchased-detail endpoint reads `unit_cost_cents ?? unit_price`. +3 tests. ⚠️ **Re-run the AP bill sync once** (`rest_ap_sync.py`, idempotent) to re-link bills synced before this. Xoro receipt mirror still qty+date only (no unit price/bill# in that feed). User-guide ch28.
> - **Inventory Snapshot — images, drill popups, date range** — the Snapshot table gained per-row **product images** (colour-matched) + **colour swatches**, row hover highlight, double row spacing + 125% font, and a **⚙ Columns** show/hide (persisted). A header **From/To date range** narrows the Sold/Purchased column totals and seeds the drills. **Sold** and **Purchased** quantities are now clickable → drill popups: **Qty Sold** (colour totals + grand + invoice list with unit price/customer/date, own date range) via `GET /api/internal/inventory-sold-detail` (sales history); **Purchased** (colour totals + receipts/bills) via `GET /api/internal/inventory-purchased-detail` (Tangerine AP vendor bills + Xoro receipt mirror). The invoice # / bill Ref # open a **full doc popup** (header + lines) with **✎ Edit (new tab)** → the AR Invoice / AP Bill. Snapshot Purchased column now also counts AP-bill qty + honours the date range. ⚠️ Historical purchase unit-price/bill# need a Xoro REST sync to enrich `ip_receipts_history` (operator model: historical=Xoro, live=Tangerine). User-guide ch28.
> - **Inventory Snapshot (Inventory Matrix default view)** — opening the Inventory Matrix with no style picked now shows a **summary table, one row per style+color** (On Hand · Allocated · On SO · ATS · On PO · ATS-incl-PO · Sold · Purchased · Item Category · In Transit · Avg Cost), paginated over the same scoped style page (brand + search apply); toggle to the stacked size matrices. New `POST /api/internal/inventory-snapshot` computes the aggregates as separate set-based queries merged in JS (no fan-out join), reusing the proven ats-by-size inbound logic. Each quantity **drills in a new tab**: On Hand→this matrix `?style_id`, Allocated→Allocations `?q`, On SO→Sales Orders `?q`, On PO→Purchase Orders `?q`, ATS/ATS-incl-PO→ATS app `/ats?style`. Added the `?q`/`?style_id`/`?style` deep-link reads to SO, Allocations, the matrix and the ATS app (PO already read `?q`). User-guide ch28.
> - **App-wide dark `<select>` default** — added a global stylesheet rule (`index.html`) giving every native `<select>` the slate/navy look on the **closed control + option popup** (`background-color #0b1220`, text `#F1F5F9`, border `#334155`; `option`/`optgroup` dark too). Builds on #1052's `color-scheme: dark`. Works via the per-property cascade: selects that set their own colors inline still win, but the many partial-inline selects (border-/padding-only, e.g. `ScaleMasterPanel`) that used to render a **generic white control** now inherit the dark colors. Scoped to `<select>` so checkboxes/radios/date pickers are untouched. No light surface in the suite carries a select (VendorApp is dark; the print view has none).
> - **SO entry — require Customer + Ship-to address; pre-fill ship-to from customer default** — the four add-styles prerequisites are now **Customer · Ship-to address · Customer PO # · Fulfillment source** (one ⚠️ banner lists whatever's missing; save also blocks on a missing ship-to). Selecting a customer **pre-fills the Ship-to from the customer master's default location** (was: only auto-selected when the customer had exactly one), and the chosen location's **address is shown** under the picker. Builds on #1327's location sync/auto-select. User-guide ch27.
> - **SO header autofill + required Fulfillment source** — on Sales Order entry: **Channel auto-fills from the customer** (Shopify-named ⇒ DTC, else Wholesale, matched by `channel_master` code); **Brand auto-populates from the selected style** (first matrix style's `brand_id`, via a new `LineMatrixBody.onPrimaryBrandChange`); Brand + Channel dropdowns now show **names only** (codes still searchable). **Fulfillment source (ATS / Production) is now required** — gates the add-style buttons + blocks save until set. The **🤖 Upload customer PO** flow **auto-picks ATS and highlights** the field for the operator to confirm. User-guide ch27.
> - **📐 Scale window → horizontal pack matrix** — the Style Master pack-ratio editor now lays sizes out **horizontally as columns** (the same orientation as the SO / PO size matrix), instead of a vertical list. Styles with no inseams show a single **Pack qty** row; styles with inseams show **one row per inseam** (each its own size curve) with a Total column per row and a column-totals footer. Pure UI re-layout of the #1320 editor — storage and downstream quick-fill unchanged. User-guide ch02.
> - **SO/PO size matrix — collapsible empty-size columns + ATS over-availability warning** — (1) once any cell carries a qty, the **first size column header turns green** and is clickable; clicking hides the all-zero columns **before the first** and **after the last** sized column (mid-range zeros stay), collapsing a wide scale to the ordered range (toggle, `⋯` markers). Both SO and PO matrices. (2) On a **Fulfillment = ATS** sales order, committing a cell qty above the available-to-ship figure pops a 3-way warning — **Continue anyway** / **Change to ATS qty** (clamp) / **Cancel** (revert). Both are opt-in props on the shared `EditableSizeMatrix` (`collapsibleSizes`, `onCellCommit`) so adjustment/transfer grids are unchanged; +3 component tests. User-guide ch27/28.
>
> **Recent cross-cutting landings (2026-06-13)** — not tied to a single phase row:
> - **Last-received dates synced from Xoro (nightly)** — inventory layers were all stamped with the 5/31 import date. The actual last-receipt date lives in the Playwright inventory snapshot ("Last Receipt Date"; the REST export has none). New `color_aliases` table (raw Xoro colour → canonical) + `sync_received_dates(jsonb)` RPC (migration `20260879000000`); `api/ats/upload` (which already ingests the snapshot nightly) now parses the raw CSV date (avoiding xlsx DD/MM mis-coercion), bridges the colour, and stamps the newest receipt date onto each SKU's Xoro-seeded layers. One-time backfill set 3,670 SKUs; recurs nightly.
> - **Style Master inseams auto-fill + per-inseam pack matrix** — (1) editing an existing bottoms style now **auto-fills the inseams it already sells** from its SKUs (read via `/api/internal/style-matrix`, the same set the Inventory Matrix shows) so the operator no longer has to re-type them; merged declared-first and persisted on the next Save. (2) The **📐 Scale** (pack ratio) window becomes a **size × inseam pack matrix** for styles that declare inseams — one pack-qty column per inseam, so each inseam carries its own size curve; pre-seeded from any existing flat pack. Storage stays backward-compatible (`size_scale_pack` is flat `{size:qty}` with no inseams, nested `{inseam:{size:qty}}` with them); the SO/PO **Qty quick-fill** resolves the matching inseam's curve per row (shared `packForInseam` in `src/shared/sizeScale.ts`, +9 unit tests). User-guide ch02/27/28.
> - **Customer style numbers on Style Master** — new `style_customer_numbers` table (migration `20260873000000`) maps one base style → each customer's own style number, so customer-customized / private-label variants stop forking a new style row per customer (the cause of the `…PL-COLOR-SIZE-<customer>` SKU sprawl). New `GET/POST /api/internal/style-customer-numbers` + `PATCH/DELETE /:id`; a self-managing **"Customer style numbers"** section in the Style edit modal (mirrors the Fabrics section). Reverse-lookup index for resolving a customer PO's number → base style (feeds the AI Upload-customer-PO flow + manufacturing module). User-guide ch02.
> - **Color Master normalized + NavDrawer fix** (#1288 + normalization) — registered the Color Master panel in `src/erp/modules.ts` so it actually appears under **Master Data → Color Master** (#1285 added the panel/route/mount but not the modules.ts entry that feeds the visible NavDrawer). **Normalized the colour picklist** 1,106 raw spellings → **857 canonical** (migration `20260868000000`, applied prod): case + spacing + size-leaked-into-colour + abbreviation (Lt/Dk/Hthr/Chrcl/Gry…) folding, proper mixed-case canonical. `styleMatrix.js` now aligns declared colours to existing SKU colours case-insensitively. ⚠️ The matching **live-SKU `ip_item_master.color` rewrite is deferred** — 84% of affected rows have NULL size/inseam with size+print baked into `sku_code`; needs a structured size/inseam backfill first (see OPERATOR-TODO 🟣).
>

> - **Color Master + declared Colors/Inseams on Style Master** (#1285) — new `color_master` table (migration `20260867000000`), **prepopulated from every distinct existing catalog colour**; new `GET/POST /api/internal/colors` + `PATCH/DELETE /:id`; standalone **Color Master panel** under Master Data → Color Master (`m=color_master`, menu key `tanda/master/colors`). Style Master gains a **Colors** multi-select (searchable chips over the master; **admins** can add a brand-new colour inline, everyone can pick existing) and an optional **Inseams** multi-value field — both stored in `style_master.attributes` (`color_ids` / `inseams`). The size-matrix lib (`api/_lib/styleMatrix.js`) now **merges declared colours + inseams into the matrix rows**, so a brand-new style (and the AI "Upload customer PO" prefill) has colour rows to fill even before any SKU exists. User-guide ch02.
>
> **Recent cross-cutting landings (2026-06-05)** — not tied to a single phase row:
> - **Allocations Workbench: per-SO Allocate/Ship/Invoice/Wave + show-all-rows fix** (#1041) — each SO sub-header now drives the whole flow in place: **Allocate** (`/allocate` RPC), **🚚 Ship** (carrier/service/tracking modal → `/ship`), **🧾 Invoice** (`/create-invoice` draft), **📦 Wave** (3PL-provider modal → `/wave` EDI-940). Status-gated with tooltips. **Show-all-rows fix:** when focused on one SO via `?so=`, the GET sends `?so=&include_all=1` so the server returns **every** line of that order (bypassing `v_allocation_demand`'s terminal-line exclusion) — previously a shipped/invoiced SO looked open-only even with the filter off. `src/tanda/InternalAllocations.tsx` + `api/_handlers/internal/allocations/index.js`. User-guide ch27 updated.
> - **Carrier Master + Ship modal SearchableSelect** — `carrier_master` table pre-populated with 16 carriers (UPS/FedEx/USPS/DHL/OnTrac/LSO/Spee-Dee/Amazon/ABF/XPO/Estes/ODFL/R+L/Maersk/Evergreen/COSCO); CRUD panel under Master Data → Carriers (`/tangerine?m=carrier_master`); Ship modal carrier input replaced with SearchableSelect backed by `/api/internal/carriers`. Menu key `tanda/master/carriers`, migration `20260830000000`. User-guide ch02.
> - **HTS Master + Fabric COO/HTS AI suggest** (#1027) — `hts_master` reference table (operator-managed HTS codes + duty rates); `country_of_origin` text column on `fabric_codes`; **🤖 Suggest** button on fabric edit modal calls Claude Haiku to propose top-3 HTS codes from fabric content + COO; new Master Data → 🛃 HTS Master panel; migration `20260828000000`. User-guide ch15 updated.
> - **Inventory Matrix: SO/PO/Invoices row-views + drill-through + ATS inventory filters** (#1040) — a **Matrix / SO / PO / Invoices** view-mode switch on a picked style: SO/PO/Invoices each render a row-driven list (all statuses) of the orders/invoices containing that style, with row-click drill-through to the real Sales Orders / Purchase Orders / AR Invoices module. New read-only endpoint `GET /api/internal/style-orders?style_id=&view=` resolves item ids → header rows with customer/vendor names pre-resolved (no uuids). Also added ATS-style **Gender / Group / Category** multi-select filters that scope the style picker (`src/tanda/InternalInventoryMatrix.tsx`).
> - **Inventory Matrix: image thumbnails + brand-level view + total-cost fix** (#1022) — per-color 44px thumbnails in each row; brand-selected / no-style shows all brand styles stacked (up to 50, parallel load); `totalCostCents` now recalculated when simple-mean avg fallback fires (was showing "—").

> **Recent cross-cutting landings (2026-06-03/04)** — not tied to a single phase row:
> - **ip_item_master dedup ✅ complete** (#867 Tier-1 zero-ref cleanup · #872 collision-aware merge + logical `UNIQUE` backstop · #874/#866 SKU-resolver dup prevention) — the long-standing ~7k duplicate-SKU problem is now resolved + guarded. (Was an operator *decision* item; now built.)
> - **Vendor onboarding / portal-access** (#864/#868/#879/#884/#886/#889, h609) — invite vendors, edit/resend invites, outstanding-invites list, view + cancel portal access (PO WIP → Onboarding).
> - **Nightly Xoro AP sync cron** (#882) — vendor-bill paid-status mirror (02:30 UTC).
> - **Costing per-line status** (#890/#877/#873/#885) — Draft/On-RFQ/Awarded/Closed per line + auto-advance + layout rework.
> - **ATS By-Size Matrix export** (#887) — color×size ATS-available pivot (see [`docs/apps/ats-overview.md`](../apps/ats-overview.md)).

---

## Phases

| Phase | Scope (modules) | Status | Refs |
|---|---|---|---|
| **P1** Foundation | M1 Tenancy · M2 GL · matrix primitive · M34 Style Master · M35 Vendor Master · M36 Customer Master | ✅ | P1 arch doc |
| **P2** Cross-cutters | M27 Approvals · M28 Notifications · M29 Documents · M30 HR/Employee | ✅ | |
| **P3** Acc Core | M3 AP · M5 Inventory FIFO · M37 Inventory Ops · M39 Mobile Scanner | ✅ | |
| **P4** AR + backfill | M4 AR (5-yr backfill) | ✅ | |
| **P5** Close + Financials | M6 Close · TB / IS / BS / CF / Year-End | ✅ | |
| **P6** Bank Recon | M7 Bank/CC Feeds · M8 Reconciliation Engine | ✅ | |
| **P7** Revenue Ops | M16 CC Capture · M17 Sales Reps & Commissions · M9-subset reporting · M47 Customer Service/Cases | ✅ | |
| **P8** Data + CRM | M25 CRM · M42 PIM | ✅ | |
| **P9** Parallel-Run | reconciliation framework + variance gate + cutover automation | ✅ | first cutover gate (Cash) ~2026-07-28 |
| **P10** Tenancy | RLS flip + entity switcher (M1 ext) | ✅ | |
| **P11** Shopify | M12 Shopify + COGS posting · **P11-10 product mirror + image unification** | 🟡 **orders ✅ / images in progress.** **M12 orders + COGS posting ✅** (order/refund/payout webhooks + 6h backfill cron + AR/COGS JE posting). **P11-10 — product mirror + image re-host (plan: `docs/tangerine/P11-10-shopify-product-mirror-and-image-unification.md`):** Wave A (#752/#753) — polymorphic `product_images` (owner_type/owner_id/source/shopify_image_id) + `shopify_products` mirror + `dropbox_backfill_failures` + `style_master.shopify_product_id` (uuid FK) + Shopify client `listProducts`/`getProduct`/`getProductImages`. **Wave B (#844 → corrected by #853):** per-style **link** (fetch product → upsert `shopify_products` mirror → set style uuid FK) + **Pull from Shopify** (re-host images via Sharp into `pim-images` as `product_images` rows, `source='shopify'`, dedup on `shopify_image_id`); **render fix** — composite GET now signs derivative URLs (no PIM image had ever rendered before — bucket-relative paths used as raw `<img src>`); UI link/pull controls in PIM Images tab (h604 link, h605 pull). **Drift migration** `20260720000000` recreates all 4 P11-10-1 objects faithfully (was applied direct-to-prod, never committed) — verified no-op on prod. **Store connect (self-serve) ✅** — **Connect Shopify Store** panel (Sales → Shopify → 🛍️ Connect Store) + `/api/internal/shopify/stores` (GET/POST/PUT/DELETE + `/test`) encrypts the Admin API token at rest (`encryptToken`) into `shopify_stores` — the previously-missing write path. **⛔ Remaining op step:** prod still has 0 stores — operator must paste a `shpat_…` token in that panel to light up orders + images. **Next (planned, not built):** P11-10-bulk — bulk **link by handle=style_code** (walk catalog, match, upsert+link, dry-run report) + **bulk pull** (batched/cursored over linked styles); 2,100 styles, 0 linked today. | M12 #515-era · P11-10 #752 #753 #844 #853 + store-connect |
| **P12** Marketplaces | M45 Marketplaces (FBA / Walmart / Faire) | ✅ | PR #515 |
| **P13** Procurement | M11 PO origination · M38 Receiving · M26 QC · M48 Trade Compliance | 🟡 **schema + M11 PO panel only** — P13-1/2 schema (all procurement tables applied, dormant) + M11 native PO panel (#732). **Receiving / QC / customs / broker / 3-way-match UI NOT built yet** (the prior "#548 UI shipped" note was stale). **Completion program in flight (C0–C5):** C0 (#799) reconciles the two PO systems — dual receipt + commitment FK so receiving attaches to EITHER a native `purchase_orders` PO or a mirrored `tanda_pos` PO; open-PO `po_commitments` recorded on native PO issue. **C1 (#801):** Receiving (`InternalReceiving`, M38) against a native PO → `tanda_po_receipts/_lines` + landed-cost rollups; Post creates FIFO `inventory_layers` (`source_kind='po_receipt'`) at landed unit cost, queues rollup AP invoices for **bookkeeper approval** (`InternalBookkeeperApproval`), consumes commitments; new **Procurement** nav group (h585–h589). (No receipt-GRNI JE yet; matched-vendor-AP-skip-layer handled in C4.) **Wave C (#804):** C2 QC inspections (`InternalQCInspections`, tanda_po_qc_inspections/_findings, pass/partial/fail + pass-rate + line dispositions; h590-h591); C3 Trade Compliance (`InternalCustomsEntries` + `InternalBrokerInvoices`, customs_entries/_lines + broker_invoices; landed-cost revaluation JE deferred; h592-h595); C4 vendor-invoice **3-way match** (`InternalThreeWayMatch`, vendor_invoice_drafts, $5/2% tolerance vs received-accepted value, approve→AP draft; h596-h597). All draft-only / no new GL-posting code (GL flows through existing AP/adjustment posting). **C5 (#805):** Procurement Reconciliation inbox + open-commitments-by-vendor report (`InternalProcurementRecon`, h598) + **period-close pre-flight blockers (D16)** — unresolved 3-way (blocking), stale customs >60d no broker invoice (blocking), failed QC (warning), added to `gl-periods/preflight.js`. ✅ **BUILD COMPLETE** — full PO→receive→QC→customs→3-way→close operational layer. **Remaining = operator-run per-vendor parallel-run cutover** (pilot: Zhejiang Zhuji Newdan) + the deferred GL refinements. **GL follow-up program in flight:** **C1 (#816) ✅** — receiving posts the goods-receipt **GRNI JE** (DR Inventory / CR GR/IR-goods 2050 / CR Accrued-Landed 2150; new `2050` account; capitalized rollup AP invoices reroute to clear 2150). **C2 (#817) ✅** — a within-tolerance 3-way match **auto-posts** the GR/IR-clearing JE on approve (DR 2050 received / DR-CR 6320 variance / CR AP), creates **no second inventory layer**; out-of-tolerance/exception drafts stay unposted for bookkeeper review (new `apInvoiceGrirMatch` rule). **C4 (#821) ✅** — **💲 Post landed cost** on a broker invoice allocates it (by value) onto a posted receipt's FIFO layers: in-stock units **revalued up** (DR Inventory), already-sold share expensed to new **Landed Cost Variance (5150)**, broker AP bill booked (CR AP); new `landedCostRevaluation` rule + `broker_invoices.tanda_po_receipt_id`. **C3 (#822) ✅** — QC failure dispositions (new `tanda_po_qc_dispositions` table + h600 endpoint + ⚖️ panel UI): **write-off** (inventory_adjustments + DR 6420 / CR Inventory; seeds 6420), **vendor credit only** (FIFO-consume + `vendor_credit_memo` + DR AP / CR Inventory via new `qcVendorCredit` rule), **vendor RMA** / **rework** (record-only). ✅ **GL POSTING PROGRAM COMPLETE** — all four deferred procurement JEs now post. **Remaining = operator-run per-vendor parallel-run cutover** (pilot: Zhejiang Zhuji Newdan). | #799 #801 #804 #805 #816 #817 #821 #822 |
| **P14** PLM ext | M32 (Design-Calendar PLM) · M33 (Tech-Pack PLM) | ⬜ | superseded in slot by ➕RBAC insertion |
| **P15** Pricing | M43 Pricing Engine | ✅ **COMPLETE** — A (#792): `price_lists` / `price_list_items` (qty breaks) / `price_promotions` schema, unified JS engine `api/_lib/pricing/engine.js` (precedence customer-own → assigned → tier → default list, + best promotion), `customers.price_list_id`, B2B catalog/orders repointed, resolve endpoint h578. B (#793): **Price Lists** + **Promotions** admin panels + customer→price-list assignment, new **Pricing** nav group (h579–h584). C (#794): internal **Sales-Order line price auto-fill** (resolve accepts SKU `item_id`→style; prefills unit $, editable, ↻ re-pull, source-list hint). User-guide ch31. (P15 *slot* itself was taken by ➕Brand Master; M43 proper now done.) **D (price-lists feature):** `price_lists.brand_id` (FK brand_master) + **per-brand DEFAULT lists** (`Default — <Brand>`, 8 brands / 650 styles) seeded at 23% margin on sell (`ceil((cost/0.77)/5)*5`) from on-hand weighted-avg cost (open-PO fallback); **Ross (431) + Burlington (224) customer lists** seeded from each customer's last invoiced price; existing `DEFAULT` kept as all-brand fallback. Cost-aware **add-a-price UX** (suggested price + Use-suggested, no-cost→cost+margin Compute, customer-list **Copy from Default**); new `/api/internal/price-lists/style-cost`. | #792 #793 #794 |
| **P16** Sales | M10 SO entry · M18 Product Allocations · M24 Showroom/Line Review · M44 Carrier | ✅ **core complete.** M10 (#698-#700) + Sales/CRM/HR batches (#701–#717) + **M18 allocations (#725)** + **M44 carrier/ship (#726)** → full draft→confirm→allocate→ship→invoice lifecycle. M24 line-review delivered as **matrix SO entry**. **Matrix initiative (#727–#733):** Size Scale master, color/size/inseam matrix on inventory view + SO entry + adjustments, **M11 native Purchase Orders** (draft→issued→in_transit→received), shared style-matrix/auto-SKU lib + scale-ordered columns. **M18 Allocations Workbench (#788):** standalone `Sales → Allocations` screen — demand grouped by style/color → SKU → competing SO lines, editable per-line allocation cells, and an Auto-allocate run (priority **factor-approved → credit-card → oldest**) with a size-level preview + a hard factor-credit gate (`apply_allocations` RPC, `v_allocation_demand` view). **Fill modes (#789):** Priority full-fill + Fair-share (pro-rata) + Capped-% (per SKU / per style-color), chosen at run time. | #698–#733 #788 #789 |
| **P17** Planning | M31 Planning/Allocations (E4 ATS foundation) | 🟡 **surfacing started.** A complete 7-phase inventory-planning app already exists at `/planning/*` (`src/inventory-planning/`: wholesale/ecom forecasting, supply reconciliation + buy recs, scenarios, accuracy+AI, execution batches, governance) — Xoro/Shopify-backed, NOT yet wired to Tangerine data. **Step 1 (#827):** surfaced it as a first-class app in the Tangerine shell — header **📈 Planning** link + a **Planning (M31)** home-landing section deep-linking the 6 screens (Wholesale/Ecom/Supply/Scenarios/Accuracy/Execution), gated by the shared `permissions.planning` flag. **Step 2 (#828) — buy plan → Tangerine PO (direction A):** the planning Execution screen gains a **🍊 Create Tangerine POs** button that turns an approved batch's `create_buy_request` actions into **draft native `purchase_orders`** (one per vendor) via `POST /api/internal/planning/buy-plan-to-po` (h601). Mapping: action `sku_id` = `ip_item_master.id` = PO line `inventory_item_id` (direct); vendor via `ip_vendor_master.portal_vendor_id` → `vendors.id` (fail-fast + skip if unlinked); unit cost from `ip_item_master.unit_cost` (→ cents). POs are draft (no number, no commitments) — operator reviews + issues them in Procurement. Idempotent (actions stamp `response_json.tangerine_po_id`); permission-gated (`run_writeback`). Reuses P13's PO model. **Step 3 (#875) — harden + verify direction A:** decision logic extracted to a pure, unit-tested core (`api/_lib/buyPlanToPo.js` + 20 tests); **Preview POs (dry-run)** button so the operator previews vendor groupings/skips before writing; **cost fallback** (item-master `unit_cost` → `ip_item_avg_cost.avg_cost` → `standard_unit_price`) so a missing master cost no longer makes a silent $0 line; coded **skip reasons** + a diagnostics summary banner (eligible / skipped breakdown / cost warnings); **vendor-link suggestions** — for an unlinked planning vendor the response matches `vendors` by code/name/alias and offers a one-click **Link** (new `link-planning-vendor` h606 sets `ip_vendor_master.portal_vendor_id`); a persistent **Tangerine PO** column on the actions table deep-links each created draft to Procurement; idempotency now re-creates if the linked draft was deleted. Prod probe found the upstream is empty (0 execution batches, `ip_vendor_master` 0 rows) and the CEO had no planning role — migration `20260725000000` grants `eran@` the planning `admin` role so the flow is usable. **OPERATOR prereqs to actually produce POs: (1) populate `ip_vendor_master` + assign vendors to buy recs, (2) create+approve an execution batch from the 7,807 recommendations, (3) link planning vendors → Tangerine vendors.** **Step 4 (#880) — direction B (supply-source CHOICE, both kept):** a planning run now picks its supply source — legacy **Xoro/ATS mirror** (default, unchanged) or native **Tangerine ERP** — via new `ip_planning_runs.supply_source` (default `'xoro'`). Both supply tables already carried `source`; the reconcile reader (`supplyReconciliationService` → `wholesalePlanningRepository.listInventorySnapshots/listOpenPos`) now FILTERS by it (`'tangerine'`→tangerine-only; `'xoro'`→`neq.tangerine`, byte-identical to before — closes the pre-existing all-sources-SUM that would double-count). New `🍊 Sync Tangerine supply` button (h607 `sync-tangerine-supply`, perm `manage_integrations`) fills the same tables tagged `source='tangerine'`: on-hand Σ`inventory_layers.remaining_qty` by item×location → `ip_inventory_snapshot`; open POs from `purchase_orders`(issued/in_transit)+lines → `ip_open_purchase_orders`. Pure transforms unit-tested (`api/_lib/planning-supply-tangerine.js`). Mapping direct (`inventory_layers.item_id` = `ip_item_master.id` = sku_id). Source selector on run creation + a supply badge on the workbench. Verified: aggregation previews **7,705 SKUs / 1,349,169 units** (ties to raw layer sum). Migration `20260727000000`. So M31 now offers BOTH directions: buy-plan→Tangerine-PO (A) and Tangerine-supply→planning (B). **Step 5 (#894) — WIP timing from Tanda milestones:** operator clarified **"inbound PO is WIP"** (the open-PO qty already IS the in-production supply, so a separate additive `wip_qty` would double-count). Tanda milestones carry stage+date only (no qty), so the refinement uses the ops-maintained **"In House / DDP"** milestone (`days_before_ddp=0`, prefer `actual_date`) as each open PO's `expected_date` in `syncOpenPosFromTandaPos` (Xoro/Tanda feed), falling back to the Xoro payload date — so the inbound PO (WIP) lands in the correct planning month. Pure `buildDdpDateMap` + 6 unit tests. Impact: of 166 open POs, 9 shift to the ops date (some across month boundaries), 157 unchanged. Server-only (no migration/UI); applies on next open-PO sync + reconcile. **Step 6 (#906) — Reports suite:** a dedicated **📊 Reports** hub at `/planning/reports` with four best-in-class analytical reports, each on-screen + one-click **Excel** export via the universal `<ExportButton>`: **Sales Performance** (wholesale by Month/Category/Customer/Channel/SKU with TY-vs-LY YoY, txn-type filter, per-SKU ABC + margin), **Inventory Health** (latest snapshot, on-hand $ at best cost, weeks-of-supply + Stockout/Low/Healthy/Excess bands), **Forecast Accuracy** (per-run volume-weighted MAPE system-vs-final + Δ + bias), **Buy Plan & Supply** (buy recs by Category/SKU/Priority with open-PO overlay, or open POs by Vendor/Receipt-month). Pure aggregators (`src/inventory-planning/reports/reports/*`) + 19 unit-tested cases; paginated client-side data (`reportsRepository`), shared `ReportTable` renderer + `/planning/reports` route; Reports nav link added across the planning workbenches. Frontend-only — no migration. **Step 7 (#1673) — filtered build scopes supply-only rows:** the forecast build's synthetic **(Supply Only)** rows (auto-added for any SKU with an open PO / on-hand but no demand pair) previously ignored the build filter entirely, so a product-filtered build (e.g. "Cargo Shorts") pulled in ~1k unrelated SKUs — denim etc. — that merely had inbound POs. The per-pair filter decision is now a pure, unit-tested helper (`pairPassesBuildFilter`, 7 cases): **product-attribute filters (style / group / sub-category / gender) apply to supply-only rows too**, while the **customer** filter still exempts them (placeholder customer = customer-agnostic inbound). Customer-only filter unchanged (keeps all supply-only). Clearing stale supply-only rows a prior full build wrote needs **Wipe + rebuild**. Frontend-only — no migration. | #827 #828 #875 #880 #894 #906 #1673 |
| **P18** B2B customer-facing | M40 B2B Customer Portal · M41 B2B Wholesale Website | 🟡 **portal live** — A foundation (b2b_accounts/b2b_price_list schema + portal-origin SO cols) · B passwordless buyer auth + session chokepoint (`resolveB2BSession`) · F internal admin (authorize buyers + price lists) · **C/D/E buyer pages**: Catalog + per-customer wholesale pricing, Cart→draft SO (`origin='b2b_portal'`, server-resolved prices, ship-to validated), Account invoices/AR + open balance + Reorder. **M43 Pricing Engine ✅** (catalog now resolves via the unified engine). **M41 storefront polish (#905):** catalog **category filter + sort + result count**, **image-ready cards** (catalog API returns a signed primary-`product_images` URL when one exists, else a branded placeholder), `min_qty` floor shown, and a header **🛒 cart chip** (units · running total). Still **inert until the operator adds prices/buyers/images** (§29.8). **Remaining: size-run ordering, online payment.** | #719–#723 #905 |
| **P19** Returns | M23 RMA / Returns | 🟡 **MVP built (#900).** Customer Returns/RMA — the reverse of the sales flow. New `sales_returns` + `sales_return_lines` (lifecycle requested→approved→received→credited) + a **Sales Returns & Allowances (4100)** contra-revenue account. Panel **Sales → ↩️ Returns/RMA** (`/tangerine?m=sales_returns`, h612/h613/h614): create RMA (customer + SKU lines), per-line **disposition** (restock / scrap), approve (assigns `RMA-YYYY-NNNNN`), receive, **issue credit memo**. Crediting REUSES the existing `ar_credit_memo` rule (no new GL): every line **DR 4100 / CR AR**; restock lines additionally **DR Inventory / CR COGS** + re-add FIFO (`source_kind='credit_memo_return'`, cost = latest layer→avg fallback); scrap lines credit-only (goods stay expensed). Pure `buildCreditMemoLines` + 7 tests. User-guide ch34. Migration `20260730000000`. **Deferred:** restocking-fee deduction (field recorded, not posted), cash refunds (credit-memo only), SO/shipment auto-population of return lines. | #900 |
| **P20** Drop-ship | M49 Drop-ship Management | 🟡 **order-management MVP (#907).** Vendor-ships-direct-to-customer orders — **no warehouse, no inventory movement**. New `drop_ship_orders` + `drop_ship_lines` (lifecycle requested→confirmed→shipped→delivered→closed; `DS-YYYY-NNNNN` on confirm; carrier/tracking; customer ship-to snapshot). Panel **Sales → 📦 Drop-Ship** (`/tangerine?m=drop_ship`, h615/h616): create (customer + vendor + lines w/ customer price & vendor cost), per-order **margin** (price − cost), status lifecycle + tracking. Migration `20260731000000`. **Accounting DEFERRED — blocked on COA:** the financial side is 2 docs (customer AR invoice revenue-only + vendor AP bill = COGS, no FIFO), but prod COA lacks standard **AR/Revenue/COGS/AP** accounts so no AR/AP can post (OPERATOR-TODO). Generate-documents step is the follow-up once the COA is seeded. | #907 |
| **P21** 3PL | M13 3PL | 🟡 **provider + shipment-tracking MVP (#911).** Contract-3PL management (distinct from marketplace FBA/WFS in P12). New `tpl_providers` (master: name/code/kind/location link/contact/account/billing notes) + `tpl_shipments` (inbound/outbound/return, lifecycle draft→in_transit→received→closed, `TPL-YYYY-NNNNN`, carrier/tracking) + `tpl_shipment_lines`. Panel **Inventory → 🚚 3PL** (`/tangerine?m=three_pl`, h617/h618/h619) with Providers + Shipments tabs. Migration `20260802000000`. **+ WAVE → EDI 940/945 (2026-06-05):** an allocated/fulfilling **sales order can be waved to a 3PL** (`POST /api/internal/sales-orders/:id/wave`) — creates an outbound `tpl_shipment` (status **released**) from allocated lines, generates an **X12 940 (Warehouse Shipping Order)** via the existing ISA/GS envelope builder (`build940`), stores it in `edi_messages` (`transaction_set='940'`, vendor_id now nullable), and **transmits** via `api/_lib/edi/transport.js` (live SFTP when the provider's `edi_protocol/edi_endpoint/edi_username/edi_credential_ref` are set + `ssh2-sftp-client` installed; otherwise generates + **queues**). The **945 (Warehouse Shipping Advice)** return path (`POST /api/internal/edi/tpl/:provider_id/inbound`, `parse945`) marks the shipment **shipped** + records tracking. SO stamped `waved_at`/`waved_tpl_provider_id`. Migration `20260833000000`. **DEFERRED:** FIFO-layer relocation to the 3PL location (layers not location-scoped yet — M52) + 3PL fee posting (COA accounts exist) + live AS2/VAN transmission (config read, send stubbed). | #911 |
| **P22** EDI | M14 EDI | 🟡 **engine pre-existed; SURFACED (#912); RESTRUCTURED.** A vendor-side X12 EDI engine was already built (`api/_lib/edi/` parser/builder/mappers/inbound-pipeline + 997 acks; outbound 850 PO + 820 payment; `edi_messages` store). #912 added the vendor dashboard. EDI is now reorganised into a **Master Data → 🔌 EDI** sub-menu: **Vendors** (`m=edi`, the original X12 panel) · **Customers** (`m=edi_customers`, `edi_customer_partners` — per-customer ISA qualifier/ID + supported docs {850,810,856}) · **Settings** (`m=edi_settings`, `edi_settings` — VAN provider/host/creds + our ISA/GS identity + test/prod toggle). Migration `20260816000000_edi_customer_van.sql`. **DEFERRED / operator setup:** AS2/SFTP/VAN **transport worker** (messages prepared+stored, not transmitted — needs VAN creds + `EDI_INBOUND_SHARED_SECRET`), live **customer/retailer transactions** (inbound 850→SO, outbound 810/856 — partners configured, transport not built), and **VAN password encryption** (placeholder plain-text field today). | #912 |
| **P23** 🚩 Xoro decommission | — (cutover milestone) | ⬜ | the practical finish line |
| **P24** Reporting | M9 full reporting · M46 BI/Analytics | 🟡 **reports pre-existed; hub added (#913).** All financial statements (Trial Balance / Income Statement / Balance Sheet / Cash Flow / Year-End — P5) + AR/AP aging + GL detail + sales reports + ~12 analytics handlers (`api/_handlers/internal/analytics/*`) were already built & surfaced. Added a **Reports & Analytics hub** (`/tangerine?m=reports_hub`, h622 `finance-kpis`): live KPI tiles (open AR/AP, inventory@cost, open SOs, current period) + grouped quick-links to every report. **DEFERRED:** folding the analytics endpoints into on-screen charts (BI dashboards) + a custom report builder. | #913 |
| **P25** Finance + API | M15 Public API · M19 Sales Tax · M20 1099 · M21 Fixed Assets · M22 Budgets | 🟡 **3 of 5 (#914): M21 Fixed Assets (register+straight-line depreciation, h623/h624), M22 Budgets (gl_budgets + vs-actual, h625), M20 1099 worksheet (h626). Mig 20260803000000. GL posting deferred. Remaining: M19 Sales Tax + M15 Public API.** | #914 |

---

## Operator insertions (➕ — ahead of the original roadmap)

These were prioritized by the operator and built out-of-sequence; they occupy the P14/P15 *slots* but are not the roadmap's original P14/P15 scope.

| Item | Status | Refs |
|---|---|---|
| ➕ **P14 RBAC** (per-module × per-action permissions, `RBAC_MODE` off→log→enforce) | ✅ | #630 #632 #634 #645 #646 #647 |
| ➕ **JWT identity bridge** (MS-OAuth → verifiable per-user token) | ✅ (live; `TANGERINE_JWT_SECRET` set) | #648 #652 |
| ➕ **P15 Brand Master** (brand + channel axes, inventory partitions) | ✅ **COMPLETE** — C1 dims + C2 switchers + C3 AR/AP aging + C4/M50 GL allocation + stock-pool (receipt #681, On-Hand-by-Pool report #685, adjustment pool #689, partition-aware consumption #692) + **Axel entity (Syndicated Apparel Group)** #686. ALL gated `BRAND_SCOPE_MODE` off → inert until operator go-live config (OPERATOR-TODO). | #650–#664 #675 #681 #685 #686 #689 #692 |
| ➕ **M50 GL Brand Allocation** (per-brand P&L accounts + %-allocation engine) | ✅ arch + A schema + B (COA UI) + C-engine (manual-JE split) + C-2 (AP-invoice split) + **D Income-Statement done** (handler enriches rows w/ brand meta + brand list; UI groups brand-children under rollup parent w/ subtotal, per-brand filter dropdown, hide-account-# toggle, brand-aware export). All gated `BRAND_SCOPE_MODE=enforce` — inert until brands configured + flag flipped. | #665 #666 #669 #670 #671 #674 #675 |
| ➕ **M51 Payroll Integration (Paycor)** | ⬜ arch (`payroll-paycor-integration-architecture.md`); **integrate, don't build** — Paycor = system-of-record (calc/withholding/e-filing/deposits/W-2); Tangerine posts the run to GL (dual-basis, `source='paycor'`) + reconciles bank draw + optional M50 labor allocation. ~2 chunks. Blocked on Paycor GL-export-vs-API access + pay-code→GL mapping. | #667 |
| ➕ **M52 Multi-Warehouse** | ⬜ planned (CEO 2026-05-31). Foundation exists: `inventory_locations` table (kinds: warehouse/fba/wfs/3pl/dropship/virtual) + `inventory_transfers`. Gaps: (1) **admin panel** to add/edit warehouses, (2) **per-location stock** — FIFO `inventory_layers` aren't location-scoped (the "advanced multi-warehouse" stretch: per-location on-hand + transfers moving qty). ~2–3 chunks when pulled in. | — |
| ➕ **Vendor default AP/expense auto-fill** (part of M50 C-2) | 🟡 done — AP-invoice entry auto-fills AP+expense accounts from the vendor's defaults; on change, prompts "set as default for this vendor?" → writes back to vendor master. (Vendor schema fields already existed.) | #672 |
| ➕ **M37 Adjustment Reason Master + GL auto-select (#1020)** | ✅ **COMPLETE** — `adjustment_reason_master` table + CRUD panel (`Master Data → 📋 Adjustment Reason Master`) with auto-coded ADJR-NNNNN codes. Inventory Adjustments (single + matrix) now: (1) pick reason from the master via searchable dropdown instead of free-text textarea; (2) GL account auto-resolved server-side to the "Inventory Adjustments Expense" expense account — no picker. Migration `20260825000000`. | #1020 |
| ➕ **M37 Transfers full build (#1024)** | ✅ **COMPLETE** — Transfers panel upgraded: (1) Style + warehouse SearchableSelect filters (replaces free-text/UUID inputs); (2) Style column resolves to `style_code` — no UUID in table; (3) **Edit** (PATCH qty/notes/date) + **Delete** buttons on unposted rows; (4) `GET|PATCH|DELETE /api/internal/inventory-transfers/:id` handler; (5) migration placeholder `20260826000000`. GL posting for cross-entity moves remains deferred. | #1024 |
| ➕ **Fabric Mill Master** | ✅ COMPLETE — `fabric_mill_master` table + full CRUD panel (`InternalFabricMillMaster`) under Master Data → Fabric Mills (`/tangerine?m=fabric_mill_master`). Server-generated `MILL-NNNNN` codes; fields: name, country_code, contact_name, contact_email, website, notes, sort_order, is_active. Column show/hide, xlsx export, row-click-to-edit, responsive modal. Migration `20260827000000`. User-guide ch02. | #1028 |
| ➕ **Carrier Master + Ship modal SearchableSelect** | ✅ COMPLETE — `carrier_master` table pre-populated with 16 carriers (parcel: UPS, FedEx, USPS, DHL, OnTrac, LSO, Spee-Dee, Amazon Logistics; LTL: ABF, XPO, Estes, ODFL, R+L Carriers; ocean: Maersk, Evergreen, COSCO). CRUD panel `InternalCarrierMaster` under Master Data → Carriers (`/tangerine?m=carrier_master`); operator-supplied codes (not auto-generated); fields: code (locked on edit), name, carrier_type (parcel/ltl/ocean/air/other), tracking_url_template, sort_order, is_active. **Ship modal upgraded:** carrier free-text → `<SearchableSelect>` backed by `/api/internal/carriers` (loaded on modal open; falls back to manual entry). Menu key `tanda/master/carriers`. Migration `20260830000000`. User-guide ch02. | #1032 |
| ➕ **Customer Buyers (replaces Contacts tab) + Buyer Scope Master + optional SO Buyer** | ✅ COMPLETE — Customer Master **Buyers** tab replaces the legacy Contacts tab. New tables: `customer_buyers` (name/phone/email/title required at API+UI; nullable in DB for legacy migration; `is_manager` flag + self-FK `reports_to_buyer_id` (CHECK no-self-report) limited to manager buyers on the same customer), `buyer_scope_master` (editable code/name, seeded 6 apparel scopes) multi-selected via `customer_buyer_scopes`, and `sales_orders.buyer_id` (optional, FK SET NULL). Per-row CRUD (`/api/internal/customer-buyers` + `/:id`) — saves immediately to avoid self-FK ordering. New panel `InternalBuyerScopeMaster` under Master Data → Buyer Scope Master (`/tangerine?m=buyer_scope_master`, menu key `tanda/master/buyer-scopes`); SO header gains an optional **Buyer** SearchableSelect scoped to the customer's buyers. Legacy `customers.contacts` migrated → buyers (idempotent) and retained as backup. Migration `20260850000000`. User-guide ch02 + ch27. | #1156 |

| ➕ **Manufacturing M1 — Part Master + Service Item Master** | ✅ COMPLETE — foundation masters for a new Manufacturing/assembly module (parts + services + labor → finished style; outsourced-CMT, actual costing, WIP). `part_master` (purchased components: blank garments/labels/trims/packaging/fabric; server `PART-NNNNN`; part_type, uom, default_vendor_id→vendors, default_unit_cost_cents, is_size_scaled, optional fabric_code_id; **no FK to ip_item_master — kept separate from style inventory**) and `service_item_master` (outsourced print/sew/pack/wash charges; server `SVC-NNNNN`; is_labor, default_vendor_id, default_charge_cents, applied_to_wip, default_expense_account_id). Full CRUD panels `InternalPartMaster` / `InternalServiceItemMaster` under Master Data (`/tangerine?m=part_master`, `/tangerine?m=service_item_master`) with vendor/fabric/account SearchableSelects, xlsx export, row-click edit, responsive modals. Migrations `20260880000000`, `20260881000000`. Menu keys `tanda/master/part-master`, `tanda/master/service-items`. User-guide ch02 + new ch44. Phases M2–M6 (part FIFO inventory, BOM, build orders + WIP postings, PO-receive-completes-build, reports) to follow. | _pending_ |

| ➕ **Manufacturing M2 — Part FIFO inventory + adjustments + GL** | ✅ COMPLETE — parts now hold real stock in their OWN FIFO pool, fully separate from finished-style inventory (never in Inventory Matrix/ATS). `part_inventory_layers` + `part_inventory_consumption` + `part_fifo_consume()` RPC (parallel to inventory_layers/inventory_fifo_consume, bound to part_master; optional location scoping). `part_adjustments` table + `part_adjustment` posting rule (positive→create layer DR 1360/CR counter; negative→FIFO-consume DR counter/CR 1360) wired through a new `postEvent` `partConsumePlan[]`/`partInventoryLayers[]` drain (calls partFifo `createPartLayer`/`consumePart`). New control account **1360 Inventory-Parts** (subledger=part). Handlers: `/api/internal/part-inventory` (on-hand by part), `/api/internal/part-adjustments` (create+post in one call) + `[id]`. UI: `InternalPartInventory` panel under new **Manufacturing** nav group (`/tangerine?m=part_inventory`) — on-hand table + Adjust/opening-balance modal, xlsx export. Migrations `20260882000000` (FIFO), `20260883000000` (GL 1360), `20260884000000` (part_adjustments). Menu key `tanda/mfg/part-inventory`. tsc + 9384-test suite + posting/FIFO integration tests + vite build all green. User-guide ch44. | _pending_ |

| ➕ **Manufacturing M3 — Bill of Materials** | ✅ COMPLETE — BOM = recipe for assembling a finished style. `mfg_bom` (finished_item_id→ip_item_master, version, status draft/active/archived, default_conversion_vendor_id; one-active-per-item partial unique index) + `mfg_bom_components` (component_kind part|service|finished_style with one-of CHECK → part_master / service_item_master / ip_item_master; qty_per_unit, scrap_pct, cost_source). Handlers `/api/internal/mfg-boms` (GET list + POST create-with-components) + `[id]` (GET decorated w/ resolved labels, PATCH header + full component replace, DELETE) + shared `_validate.js`. UI `InternalMfgBom` under Manufacturing (`/tangerine?m=mfg_bom`): BOM list + editor modal with async style picker (`/api/internal/items?q=`) + component matrix (kind→part/service/finished-style picker, qty/scrap/cost), frozen footer, xlsx export. Migration `20260885000000`. Menu key `tanda/mfg/boms`. tsc + parity/manifest tests + vite build green. User-guide ch44. | _pending_ |

| ➕ **Manufacturing M4 — Build orders + WIP postings** | ✅ COMPLETE — the accounting core. `mfg_build_orders` (build_number BUILD-NNNNN; finished_item_id, bom_id, target/completed qty, status draft→released→issued→completed/cancelled, wip_account_id, accumulated_cost_cents, finished_unit_cost_cents, conversion_po_id placeholder for M5) + `mfg_build_components` (snapshot; part/service/finished_style one-of; qty_required/consumed, actual_cost_cents, service_charge/vendor/invoice/capitalized). New GL: **1305 WIP** (asset CONTROL, subledger=build_order) + **2160 Accrued CMT**. Added `manufacture` to inventory_layers source_kind (+ fifo.js). Three postEvent rules: **mfg_build_issue** (parts+styles→WIP at FIFO via partConsumePlan/consumePlan indexed drains, parts-first ordering for drop-safety), **mfg_service_capitalized** (DR WIP/CR AP), **mfg_build_complete** (DR finished-inv/CR WIP + creates finished FIFO layer at accumulated/qty). Handlers `/api/internal/build-orders` (list+create) + `[id]` (GET w/ WIP rollup, PATCH cancel, DELETE) + `[id]/release|issue|service|complete`. UI `InternalMfgBuildOrders` under Manufacturing (`/tangerine?m=mfg_build_orders`): list + build detail (component matrix, live WIP rollup, lifecycle buttons, per-service capitalize via promptDialog). Migrations `20260886000000` (+constraint), `20260887000000` (GL). 11 new rule unit tests + tsc + integration/parity + vite build green. Menu key `tanda/mfg/build-orders`. User-guide ch44. | _pending_ |

| ➕ **Manufacturing M5 — Conversion-PO receipt completes a build** | ✅ COMPLETE — answers the operator's "PO for the printed tee — how is it received against the PO?". `tanda_po_receipts.build_order_id` + real FK `mfg_build_orders.conversion_po_id`→purchase_orders (mig `20260888000000`). Extended `api/_handlers/internal/procurement/receipts/post.js`: when a receipt (or its PO via `mfg_build_orders.conversion_po_id`) is build-linked, it SKIPS the normal GRNI/landed path and fires `mfg_build_complete` — moves accumulated WIP → finished-goods inventory at real build cost (DR style-inv / CR 1305 WIP + finished FIFO layer at accumulated÷received-qty, source_kind=manufacture), flips build to completed + completed_qty = accepted qty, stamps receipt. Guards: build must be 'issued' + all services capitalized. Reuses resolveInventoryAccount + postEvent. node --check + posting-rules/mfg-rule tests + tsc green. ⚠️ native purchase_orders currently 0 rows (forward-looking infra). Part-purchasing-via-AP deferred (parts stocked via M2 adjustments today). User-guide ch44. | _pending_ |
| ➕ **Manufacturing M6 — Reports** | ✅ COMPLETE — closes the epic. Read-only `GET /api/internal/mfg-reports` aggregates: **open WIP** (builds in released/issued/in_progress with parts/service/style cost split + days-open + WIP total), **completed builds** (qty, cost breakdown, total + finished unit cost, complete date), **parts valuation** (top parts by on-hand value from part_inventory_layers + total). UI `InternalMfgReports` under Manufacturing (`/tangerine?m=mfg_reports`): 3 summary cards + 3 exportable sections. Menu key `tanda/mfg/reports`. tsc + parity/manifest + vite build green. User-guide ch44. **Manufacturing epic M1–M6 COMPLETE** (#1300/#1305/#1306/#1310/#1311 + this). | _pending_ |

| ➕ **Manufacturing M5b — Buy parts onto a vendor bill** | ✅ COMPLETE — the deferred half of M5 (part purchasing). Extended `apInvoiceReceived` with a **part line** (`{part_id, part_inventory_account_id, qty, unit_cost_cents}` → DR 1360 subledger=part / CR AP subledger=vendor + queues a `partInventoryLayers[]` entry; dropped on credit memos). New `invoice_line_items.part_id` (mig `20260889000000`). New `GET/POST /api/internal/part-purchases`: POST creates a real **vendor bill** (mirrors ap-invoices create: gl_status draft→posted, total trigger) + a part line and posts it via `ap_invoice_received`, stocking the part at purchase cost with a payable to the vendor; GET lists recent part-purchase bills. UI: **+ Receive purchase** / per-row **Buy** in `InternalPartInventory` (part+vendor SearchableSelect, qty, unit cost, optional bill #). 5 new ap-part-line unit tests + AP/posting/parity (108) + tsc + vite build green. User-guide ch44. Manufacturing epic now fully closed incl. part procurement. | _pending_ |

| ➕ **Manufacturing — Part Type Master + app-colored dropdowns** | ✅ COMPLETE — promoted part_master.part_type from a hardcoded enum to an operator-managed master. New `part_type_master` (operator code + name; seeded 6 originals; mig `20260890000000` also DROPS the part_master_part_type_check so master-driven types are accepted). Handlers `GET/POST /api/internal/part-types` + `[id]` (delete blocked if any part uses the type). UI `InternalPartTypeMaster` under Master Data (`/tangerine?m=part_type_master`); Part Master "type" is now a master-backed **SearchableSelect**. ALSO converted every remaining native `<select>` in the manufacturing panels (service kind, adjustment type, BOM status/component-kind/cost-source) to the dark `SearchableSelect` so all dropdown option lists render in app colors. part-master handler validation relaxed (no hardcoded enum). Menu key `tanda/master/part-types`. tsc + parity + vite build green. User-guide ch02. | _pending_ |

| 🐛 **Fix: new-tab drills no longer re-prompt Microsoft sign-in** | ✅ COMPLETE — clicking a quantity in the Inventory Snapshot (On Hand / Allocated / On SO / ATS / On PO) — and the Vendor/Customer Scorecard drills — opened the target in a new tab via `window.open(url, "_blank", "noopener")`. The `noopener` flag makes the browser start the new tab with an **empty sessionStorage**, so the PLM session (`plm_user`) was absent and Tangerine fell back to its primary Microsoft-365 identity → a spurious "sign in with Microsoft" prompt. Dropped `noopener` from the three same-origin `openTab`/`openRecordInNewTab` helpers (and the `<QtyLink>` anchor's `rel`) so the drill tab inherits the opener's session — matching how NavDrawer's right-click "open in new tab" already behaves. Same-origin only, so no reverse-tabnabbing exposure. `InternalInventoryMatrix`, `VendorScorecard`, `CustomerScorecard`. typecheck ratchet green. | _pending_ |

| ➕ **Drill-through Phase 1 — every number descends to the journal entry (QuickBooks-grade)** | ✅ COMPLETE — closes the chain's missing bottom half (TB/IS/BS→GL-detail→JE already worked). NEW: (1) **JE → source document**: `GET /api/internal/journal-entries/:id/source` resolves `source_table`/`source_id` (by-convention strings, no FK — this is THE switch) → `{label, module, q}`; the JE modal's Source row becomes a jump link (AR invoices, AP bills, payments, receipts, adjustments, commissions, build orders; mirror summaries label as runs). (2) **Related-entry links**: sibling/reverses/reversed-by numbers re-load the modal in place (`jumpId` state). (3) **Standalone GL Detail panel rows → JE modal** (was the one dead end vs the modal variant). (4) **Reverse chain**: AR-invoice + AP-bill status badges and AP-payment "✓ posted" cells link to their posting JEs (`accrual_je_id`/`cash_je_id` were fetched-but-unrendered). (5) **Deep link `?m=journal_entries&je=<id>`** auto-opens the entry modal (one-shot). `DrillModuleKey` union extended. Also: routes-manifest coverage test given a real timeout (loads 700+ handlers; 5s default started flaking at 708 routes). Phase 2 planned: agings, Segment P&L→GL (routed revenue makes cells map to accounts), bank recon, scorecards. User-guide ch03. | _pending_ |

| ➕ **DR posture: PITR enabled + restore drill PASS + runbook** | ✅ COMPLETE — closes the audit's backup/DR gap. **PITR (7-day, $100/mo) enabled on prod 2026-07-08** via Management API (RPO ~2 min, was ≤24h daily-backup only; dailies continue for the longer tail). **Restore drill PASS in ~10s** — `npm run dr:drill` (`scripts/dr-drill.mjs`, repeatable/quarterly): extracts the books (gl_accounts + journal_entries + lines) from prod, restores into an isolated `dr_drill_<date>` schema on STAGING, verifies counts + the debit/credit checksum **to the cent** ($547,032.58 = $547,032.58). **`docs/tangerine/DR-RUNBOOK.md`**: three restore paths (PITR-in-place destructive w/ freeze-writers steps; restore-to-new-project; surgical row copy-back), drill log, known gaps (no local pg_dump → weekly off-provider dump follow-up; stale staging `db.*` DATABASE_URL — use pooler/Mgmt-API; 21:00 fetch single-PC — Mac warm-standby is an operator action). | _pending_ |

| ➕ **Revenue→GL Phase 4 — AR history + daily Xoro payment-state receipts (Option A)** | ✅ COMPLETE — history loads now AND AR tracks Xoro daily. (1) **P4-8 backfill runner routed**: `ar-backfill/run.js` now routes every historical line via `revenueRouting.js` (brand×gender×store-channel×PL → 4005-4012 + COGS twins from `unit_cost_at_sale`), AR header/JE by customer class (1107/1105/1108 with customer subledger), stale fallbacks realigned, T11 `audit_reason` stamped (it would have been rejected without it). (2) **Payment state**: new `ar_xoro_payment_state` (mig `20260963000000`) + `POST /api/sales/upload-payment-state`; `rest_invoice_sync.py` collects `StatusName`+`FullPaymentDate` per invoice during its existing walk (pre-window, full coverage) and pushes nightly (`post_payment_state`; `--payment-state-only --walk-mode head` = historical catch-up). (3) **Receipts reconciler**: daily `/api/cron/ar-receipts-reconcile` (05:00 UTC) posts `DR 1051 Factor Advances - Rosenthal` (factored) / `DR 1030 Undeposited Funds` (other) / `CR invoice AR` (customer subledger) for invoices Xoro marks paid — `journal_type ar_receipt_xoro`, posting_date=FullPaymentDate clamped to the open window, idempotent per invoice, error-tracking wired, batch 400/run. Sync Health gains the `ar_payment_state` feed. 6 unit tests (paid classification, date clamping, payload). | _pending_ |

| ➕ **Revenue→GL Phase 3 — native invoice lines routed by the COA spec** | ✅ COMPLETE — `sales-orders/create-invoice.js` now stamps each draft line's revenue + COGS account via the shared `revenueRouting.js` resolver (style brand + gender + PL suffix + SO channel). Precedence: **explicit `style_master` account override → resolver → SO line → customer default → entity default** — so revenue lands in 4005-4012 (+ COGS twins) instead of one catch-all the moment native invoicing starts. AR header chain now includes the CUSTOMER's class default (factored 1107 / CC 1105 / house 1108, stamped 2026-07-07) between the SO override and the entity default. New adapter `channelFromChannelMasterCode`: DTC → the item brand's store (PT → psychotuna.com bucket, else ringoffireclothing.com); FBA/WALMART/FAIRE → wholesale until they get their own revenue accounts. OPEN: native sample-order detection signal undefined (samples currently route like normal sales — the bridge's 4010 path is bridge-only until the CEO defines the SO-level signal). | _pending_ |

| ➕ **Revenue→GL Phase 2 — bridge daily revenue JEs ROUTED by brand×gender×store** | ✅ COMPLETE — the mirror's AR summary is no longer a single lump. New `api/_lib/xoro-mirror/ar-routed-summary.js` (pure core + IO loader): DR one line per (AR account × customer) — factored 1107 / CC 1105 / house 1108 with **customer subledger** (REQUIRED: those are control accounts post-restructure; the JE guard rejects bare control lines — the old lump would have started failing) / CR one line per revenue bucket (4005/4006/4007/4008/4009/4011/4012) resolved per invoice line from style brand+gender+PL suffix and the day's Xoro store→channel (`ip_channel_master` codes ROF/ROF ECOM/PT/PT ECOM — ecom sales ARE in `ip_sales_history_wholesale`, CEO-confirmed store=channel; ~$534k ROF ecom + $174k PT ecom in-feed). Remainders/SKU-less lines route to the channel bucket so the JE always balances (throws if not). Resolver refinement (CEO-confirmed): **ecom accounts are STORE-scoped** — store beats item brand (ROF-brand item on psychotuna.com → 4008); PT wholesale stays brand-scoped (beats kids gender). Bridge COGS stays PERIODIC (AP purchases + inventory Δ) until native cutover. 19 unit + adapted integration tests (mock gained `.in()`). | _pending_ |

| ➕ **Revenue→GL Phase 1 — routing resolver + COA realignment of every posting fallback** | ✅ COMPLETE — first chunk of making the GL the source of record for revenue. New `api/_lib/accounting/revenueRouting.js`: ONE resolver mapping (brand × gender × channel × PL/sample/shipping/consignment flags) → the operator's 2026-07-07 revenue COA (4005 catch-all / 4006 Kids B·C·G / 4007 consignment / 4008-4009 PT ecom·wholesale / 4011 ROF ecom / 4012 PL / 4010 samples **NO COGS** / 4014-4016 shipping income) + COGS twins (5010/5011/5012/5013/5014/5015/5018), plus `resolveArAccountCode` (factored 1107 / CC 1105 / house 1108) — 50 unit tests. CRITICAL REALIGNMENT: the COA restructure broke every hardcoded account fallback — `ar-invoices/post.js` ('1200'→1108, '4000'→4005, '5000'→5010, '1300'→1201), `xoro-mirror/summary-je.js` ('1200/4000/5000/2100/1300'→1108/4005/5001/2000/1201; 2100 is now a non-postable Credit Cards header, 2000 = AP control), entity defaults in prod (default_revenue 4000→4005, default_cogs 5000→5010). Next: Phase 2 = bridge daily revenue JEs routed via the resolver from ip_sales_history grain; Phase 3 = create-invoice stamps line accounts via the resolver. ⚠️ ip_sales_history_ecom is EMPTY — ecom revenue (4008/4011/4014/4015) has no bridge source yet. User-guide ch22 diagram updated. | _pending_ |

| ➕ **App-wide error tracking + internal-token/crypto fail-closed** | ✅ COMPLETE — the audit's remaining P0s. `app_errors` (mig `20260962000000`, service-role-only) captures: dispatcher catch-all (`source='api'`, all ~700 dispatched routes), browser uncaught errors/rejections (`src/utils/errorReporter.ts` → `/api/internal/client-errors`, per-session cap+dedupe+batch, `keepalive`), opt-in cron capture. Fingerprinted (uuid/number-normalized, tested) so repeats group; daily `app-errors-digest` cron 13:05 UTC sends ONE bell+email of top groups (silent when clean) + prunes >30d. SECURITY: `authenticateInternalCaller` now **fails CLOSED (503) in production** when `INTERNAL_API_TOKEN` is missing (verified the token IS set in prod — the audit's "fail-open" was the unset-env path; this guards regressions); `api/_lib/crypto.js` **rejects weak/malformed `VENDOR_DATA_ENCRYPTION_KEY`** instead of silently scrypt-deriving (prod key verified 64-hex/32B → fallback was dead code; NEVER rotate the key). User-guide ch22 §22.13. | _pending_ |

| ➕ **Xoro bridge hardening — SO push sync, stale-guard contract, Sync Health + alerts** | ✅ COMPLETE — closes the 2026-07-07 bridge audit findings (tanda_sos stale 19d; accounting mirror skipped 37/40 nights on an EMPTY xoro_sync_logs; zero alerting). (1) `sync-from-xoro` now writes the `xoro_sync_logs` completion row the mirror's 25h stale-guard reads — the guard finally has its signal. (2) SOs flipped from pull to PUSH: new `POST /api/tanda/upload-sos` (gzip-chunked raw records, server does no Xoro I/O; the pull twin 504'd at ~12k SOs) + `rof_xoro_project/rest_sales_orders_sync.py` posts what its nightly walk already fetched (`post_tanda_sos`, `--skip-tanda-upload` opt-out). (3) `v_xoro_feed_health` (mig `20260961000000`, 8 feeds, ok/stale/never) + daily `xoro-feed-health-alert` cron (13:00 UTC; ONE bell+email listing every non-ok feed) + **Admin → Sync Health** panel (`?m=sync_health`, export) + `npm run sync-health` CLI. Bonus root-fix: `gen:module-keys` had silently rotted pointing at Tangerine.tsx after the MODULES→erp/modules.ts refactor — mirror unfrozen 123→132 modules (User Access grid was missing 9 panels). User-guide ch22 §22.12. | _pending_ |

| 🐛 **Fix: PO matrix ignored the assigned size scale — phantom duplicate columns (ROF-P001044)** | ✅ COMPLETE — legacy SKU size spellings (`SML/MED/LRG/XLG/XXL`, `S/M/L/XL`; 44 styles / 32 active POs / 814 lines carry them) seeded the PO/SO/AR modal matrix at their RAW spelling while the payload keys columns + sku cells by the backend canon (`SMALL…2XLARGE`), so a legacy-spelled order rendered empty scale columns PLUS phantom duplicate columns, missed per-cell avg-cost/on-hand lookups (projected margin silently fell to the 21% fallback), made an extra resolve-sku round-trip per cell on save, and split columns in the PO View doc. Fix = new shared `canonSizeLabel` (`src/shared/sizeSort.ts`, exact mirror of api/_lib/styleMatrix.js `normalizeSize`, KEEP IN SYNC) applied at the two missed seams: the modal seed (`LineMatrixBody` — symmetric to the existing canonColor seed line) and the PO grid ▸expander grouping (`InternalPurchaseOrders`). Catalog rows NOT mutated (presentation-canon design); saves resolve to existing SKUs via the payload's canonical cell map. +7 unit tests. User-guide ch28. | _pending_ |

| 🐛 **Fix: PO data-quality — coverage warnings 12 → 0 + verified-suppression mechanism** | ✅ COMPLETE — closed the last `incomplete_size_coverage` findings (post-#1643). (1) **Colour-spelling fragments** (source spells one colour two ways: `WOODLAND CAM`/`CAMO`, `Lt/Light Brown`, `GD/Gd`): added `CAM→CAMO` to the `canonColor` abbreviation map (both mirrors + test), ran `scripts/canonicalize-colors.mjs --apply` (38 rows), and repointed 4 PO-line bindings whose canonical twins already existed (`uq_ip_item_master_logical_sku` blocks rename-into-dup; pairs join the known #2b population). (2) **`ROF-P001265` wrong-style bindings**: importer's sibling auto-create drew from the whole `STYLE-*` code family, so `rbb0185-Navy/Charcoal` lines bound to `RBB0185-03SF(PPK)` variant styles via the 23505 tuple fallback — resolver now restricts siblings to the parsed style's own `style_id`; new **`--po=<number>`** targeted re-import flag; re-imported (9 plain-style SKUs created, `TOTAL_MISMATCH` 0). (3) **`po_dq_suppressions`** (mig `20260960000000`): operator-verified false positives recorded with a mandatory reason and anti-joined out of `v_po_data_quality` — seeded with the one genuine single-size order (`ROF ECOM-P001016` RYB1338 `BLK CAMO` LRG-only, verified vs source). audit-pos money invariants stayed green. User-guide ch28. | _pending_ |

| 🐛 **Fix: remaining new-tab openers that dropped the PLM session** | ✅ COMPLETE — sweep after the drill fix. Same `noopener`-drops-sessionStorage defect in four more **same-origin** internal openers: (1) Tangerine top-bar module menu — `<a rel="noopener">` + the modal-open `window.open("?m=…","_blank","noopener")` (ctrl/middle-click or open-with-modal → fresh Microsoft prompt); (2) NavDrawer **"All Apps" switcher** (`SUITE_APPS`) — opened PO WIP (`/tanda`, username/password gate) and Design Calendar (`/`) logged-out; (3) NavDrawer **Planning** link (`/planning/wholesale`); (4) Costing RFQ-list row → `/costing?project=` drill. Dropped `noopener` (anchors → `rel="noreferrer"`; `window.open` → no 3rd arg) so each inherits the opener's session — matching the PLM launcher (`PLM.tsx`) + NavDrawer right-click, which already omit it. External links (image thumbnails, attachments, vendor-portal `/vendor`, Supabase dashboard) correctly KEEP `noopener`. Note: Costing/Planning/ATS/GS1 only *degraded* (unscoped notifications bell) since they fail-open; Tangerine + PO WIP are the ones that actually re-prompt login. `Tangerine`, `NavDrawer`, `RfqListView`. typecheck ratchet green. | _pending_ |

---

## Modules (M1–M49) quick index

✅ **Built (~25):** M1 Tenancy · M2 GL · M3 AP · M4 AR · M5 Inventory FIFO · M6 Close · M7 Bank Feeds · M8 Recon · M9 (subset) · M12 Shopify · M16 CC Capture · M17 Commissions · M25 CRM · M27 Approvals · M28 Notifications · M29 Documents · M30 HR · M34 Style Master · M35 Vendor Master · M36 Customer Master · M37 Inventory Ops · M39 Scanner · M42 PIM · M45 Marketplaces · M47 Cases

🟡 **Partial (4):** M11 PO · M26 QC · M38 Receiving · M48 Trade Compliance (all P13)

✅ **M10 SO** complete (P16): entry panel + API, draft→confirm→AR invoice, factor approval, multi-store split. **M18 Allocations** complete: per-SO reserve (#725) + standalone **Allocations Workbench** (#788, cross-SO priority full-fill + factor-credit gate). **M44 Carrier** complete (#726).
⬜ **Not started:** M15 API · M19 Sales Tax · M20 1099 · M21 Fixed Assets · M22 Budgets · M23 RMA · M24 Showroom · M31 Planning · M32/M33 PLM ext · M40/M41 B2B · M46 BI · M49 Drop-ship

---

## How to update this doc

When a phase or module lands (or partially lands), in the SAME PR:
1. Flip its row status (⬜→🟡→✅) + add the PR number(s) in Refs.
2. Re-count the Summary table (done phases / 25, done modules / 49) and update the three %s.
3. Bump **Last updated**.

Keep it honest — 🟡 for "arch/partial," ✅ only when the phase is functionally shippable. The % is derived from the rows, so the rows are the source of truth.
